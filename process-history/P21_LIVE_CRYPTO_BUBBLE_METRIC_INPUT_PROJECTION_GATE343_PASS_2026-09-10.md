# P21 Live Crypto Bubble Metric-Input Projection — Gate343 PASS

## Canonical promotion

Fresh GitHub re-proof on 2026-09-10 established `Kairos Controlled Roadmap Gate` #343 / run `34434160306` / job `102735671766`, exact head `cd0629905d3db6325e38c283e540e101002ee55e`, as FULL SUCCESS and therefore the latest canonical GOLDEN.

Every required job stage completed successfully: exact archive identity/integrity, authoritative Gate342 base/candidate extraction, exact six-file Live Crypto Bubble metric-input projection scope, deterministic install, exact Lightweight Charts 5.2.1 dependency proof, production TypeScript compilation, production build, dedicated Bubble metric-input verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, `KAIROS_CURRENT_CANDIDATE` upload, and `KAIROS_GATE_EVIDENCE` upload.

Exact canonical candidate: `KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_FOUNDATION_CANDIDATE_2026-09-10.zip`; size `1,595,701` bytes; SHA-256 `abaf94beb36d46e1e33ffff8b7e6bbd93db785a7260c132d2039b7cf1cee059a`; root exactly `kairos_p76/`; repository-root Git blob `727fc3c1aa40181ee807fcb743bef2ec2c541199`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10135782802`, wrapper size `1,366,212` bytes, digest `sha256:7a011e6792bef470e92c1576da7ee0806dbe9203477090dc8771e61b8da47873`.
- `KAIROS_GATE_EVIDENCE`: id `10135783064`, wrapper size `1,218` bytes, digest `sha256:e336e2d2afbeee9a9baa91ee1374eaa8a9ead66b561414f6787562b076ff30fd`.

## Released responsibility

Gate343 canonically releases `src/services/market-data/liveCryptoBubbleMetricInputProjection.ts` as the provider-neutral owner for composing already-released freshness truth into the two numeric metric inputs approved for the Live Crypto Bubble Map.

The owner accepts the released Gate338 freshness-evaluation result, preserves successful evaluation time and exact scoped entry order/instrument/fact/age/freshness association, keeps missing facts explicit with null metrics, exposes exact canonical `quoteVolume24h` for Bubble size truth, and delegates present-fact 24h percentage movement to the released Gate342 owner. Upstream freshness failure and delegated movement failure both fail closed with deterministic released reasons. It does not rank, color, size geometrically, filter, render, or mutate market/session truth.

## Post-PASS reconciliation

Fresh engineering `main` was still at the Gate343 verification head before reconciliation, and the latest Actions state showed Gate343 as the newest canonical run with FULL SUCCESS and no competing canonical chain.

Architecture ownership was reconciled through `docs/KAIROS_GATE343_ARCHITECTURE_ADDENDUM.md` on main commit `94bff515028a2bf1fa4c39e59bb8a154da8d45e3`.

## Next dependency proof

Fresh Gate343 source leaves one direct, provider-neutral seam below presentation. Gate340 emits each resolved Home lifecycle result as `HomeDashboardLiveMarketSummaryFreshnessObservation`, preserving `acquisitionResult` separately from `freshnessEvaluation`. Gate341 wires that observer into the Binance Home runtime and exposes the Gate340 observation-sink slot. Gate343 now converts the exact `freshnessEvaluation` into Bubble numeric metric truth, but production source has no consumer connecting Gate340 observations to Gate343. `HomeRoute` remains a presentation placeholder and must not become the business/data composition owner.

The next smallest dependency-safe responsibility is therefore a **Home Dashboard Live Crypto Bubble metric-observation bridge below React presentation**.

The next contract is frozen from current source conventions:
- create one application-layer adapter `createHomeDashboardLiveCryptoBubbleMetricObservationSink(...)` returning the released Gate340 sink shape;
- on each upstream observation, pass exactly `observation.freshnessEvaluation` exactly once to Gate343 `projectLiveCryptoBubbleMetricInputs(...)`;
- emit the exact original freshness-observation object plus the exact Bubble metric projection as separate fields;
- keep deterministic Gate343 `{ ok: false }` results as observation data, not lifecycle errors;
- forward upstream `onError(error)` unchanged;
- do not catch/reinterpret downstream callback failures; and
- introduce no clocks, thresholds, ranking, provider/acquisition/session mutation, color/geometry/visibility policy, persistence, React route state, Your Trades/journal/chart/navigation/transition ownership.

No next candidate/helper/canonical gate was started in this promotion checkpoint. Candidate construction should begin only from exact Gate343 GOLDEN and should use the established deterministic GitHub helper/reconstruction path with exact controlled scope and full required regressions before any canonical gate retarget.
