name: P2.1 Design System Foundation Gate

on:
  workflow_dispatch:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  verify-p2-1:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Extract P2.1 candidate
        run: |
          unzip -o KAIROS_P2_1_DESIGN_SYSTEM_FOUNDATION_CANDIDATE_2026-08-31.zip -d _extracted
          SRC_DIR=$(find _extracted -maxdepth 3 -name package.json -exec dirname {} \; | head -n1)
          test -n "$SRC_DIR"
          shopt -s dotglob
          cp -r "$SRC_DIR"/* .
          rm -rf _extracted

      - name: Use locked Node toolchain
        uses: actions/setup-node@v6
        with:
          node-version: "22.16.0"
          package-manager-cache: false

      - name: Re-run authoritative P1 regression gate
        run: npm run release:p1

      - name: Run P2.1 design-system contract
        run: npm run verify:p2:design-system

      - name: Upload P1 regression evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: P2_1_P1_REGRESSION_EVIDENCE
          path: P1_GATE_EVIDENCE.json

      - name: Upload verified package lock
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: P2_1_PACKAGE_LOCK
          path: package-lock.json
