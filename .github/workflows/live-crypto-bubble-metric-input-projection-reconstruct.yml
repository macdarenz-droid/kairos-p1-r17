name: Kairos Live Crypto Bubble Metric Input Projection Reconstruction

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/live-crypto-bubble-metric-input-projection-reconstruct.yml'

permissions:
  contents: write

concurrency:
  group: kairos-live-crypto-bubble-metric-input-projection-reconstruct
  cancel-in-progress: false

jobs:
  reconstruct-and-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - name: Pin npm
        run: |
          npm install --global npm@10.9.2
          test "$(npm --version)" = "10.9.2"

      - name: Reconstruct and verify Live Crypto Bubble metric-input projection from Gate342 GOLDEN
        shell: bash
        run: |
          set -euo pipefail
          BASE_ZIP=KAIROS_LIVE_MARKET_SUMMARY_24H_PERCENTAGE_MOVEMENT_FOUNDATION_CANDIDATE_2026-09-10.zip
          OUT_ZIP=KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_FOUNDATION_CANDIDATE_2026-09-10.zip
          PAYLOAD=.github/kairos-helper-payloads/live-crypto-bubble-metric-input-projection-overlay.b64
          WORK="$RUNNER_TEMP/live-crypto-bubble-metric-input-projection-work"
          BASE="$RUNNER_TEMP/live-crypto-bubble-metric-input-projection-base"
          OVERLAY="$RUNNER_TEMP/live-crypto-bubble-metric-input-projection-overlay.tar.gz"
          test -f "$BASE_ZIP"
          test "$(stat -c%s "$BASE_ZIP")" = "1590138"
          echo "bdc7c8ed86f69c0fa877da04964de242beecabca3430c84aeebd4f0f1be086cf  $BASE_ZIP" | sha256sum -c -
          test -f "$PAYLOAD"
          base64 -d "$PAYLOAD" > "$OVERLAY"
          echo "b6cd7eac738eed3aad65fad0472869a8bdb647348b0dd4f8eb8c8b720da4807b  $OVERLAY" | sha256sum -c -
          rm -rf "$WORK" "$BASE"
          mkdir -p "$WORK" "$BASE"
          unzip -q "$BASE_ZIP" -d "$WORK"
          unzip -q "$BASE_ZIP" -d "$BASE"
          ROOT="$WORK/kairos_p76"
          test -f "$ROOT/package.json"
          test -f "$ROOT/src/services/market-data/liveMarketSummaryFreshnessEvaluationProjection.ts"
          test -f "$ROOT/src/services/market-data/liveMarketSummary24hPercentageMovement.ts"
          test ! -e "$ROOT/src/services/market-data/liveCryptoBubbleMetricInputProjection.ts"
          tar -xzf "$OVERLAY" -C "$ROOT"

          cd "$ROOT"
          npm ci
          test "$(node -p "require('./node_modules/lightweight-charts/package.json').version")" = "5.2.1"
          npm ls lightweight-charts --depth=0
          npm run verify:live-crypto-bubble-metric-input-projection-foundation
          npm run verify:live-market-summary-24h-percentage-movement-foundation
          npm run verify:live-market-summary-freshness-evaluation-projection-foundation
          npm run typecheck
          npm run build
          npx vitest run \
            tests/live-crypto-bubble-metric-input-projection-foundation.test.ts \
            tests/live-market-summary-24h-percentage-movement-foundation.test.ts \
            tests/live-market-summary-freshness-evaluation-projection-foundation.test.ts \
            tests/live-market-summary-fact-contract.test.ts \
            tests/percentage-calculator.test.ts \
            tests/decimal-kernel.test.ts
          npm test
          node - <<'NODE'
          const {readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process');
          const p=JSON.parse(readFileSync('package.json'));
          const scripts=Object.keys(p.scripts||{}).filter(x=>x.startsWith('verify:')).sort();
          for(const required of [
            'verify:live-crypto-bubble-metric-input-projection-foundation',
            'verify:live-market-summary-24h-percentage-movement-foundation',
            'verify:live-market-summary-freshness-evaluation-projection-foundation',
            'verify:binance-home-dashboard-live-market-observed-runtime-composition-foundation',
            'verify:home-dashboard-live-market-summary-freshness-observation-bridge-foundation',
            'verify:p20:5-saved-analysis-system-closure','verify:p19:7-risk-reward-tool-system-closure',
            'verify:p18:60-drawing-tools-system-closure','verify:p17:16-chart-engine-system-closure',
            'verify:p16:18-crypto-live-feed-system-closure','verify:p15:14-market-data-adapter-system-closure',
            'verify:p14:9-trade-visualizer-system-closure','verify:p11:calculation-brain-closure','verify:p8:navigation-shell','verify:p1',
          ]) if(!scripts.includes(required)){console.error('Missing '+required);process.exit(1)}
          for(const name of scripts){const r=spawnSync('npm',['run',name],{stdio:'inherit'});if(r.status!==0)process.exit(r.status||1)}
          NODE

          rm -rf node_modules dist coverage .vite .cache
          find . -name '*.tsbuildinfo' -delete
          find . -name '*.log' -delete

          python - <<'PY'
          from pathlib import Path
          import hashlib, os
          base=Path(os.environ['BASE'])/'kairos_p76'
          work=Path(os.environ['WORK'])/'kairos_p76'
          forbidden={'node_modules','dist','coverage','.vite','.cache'}
          for p in work.rglob('*'):
            assert not any(part in forbidden for part in p.parts), f'forbidden residue: {p}'
            assert not p.name.endswith('.tsbuildinfo') and not p.name.endswith('.log'), f'forbidden file residue: {p}'
          def h(p): return hashlib.sha256(p.read_bytes()).hexdigest()
          x={str(p.relative_to(base)):h(p) for p in base.rglob('*') if p.is_file()}
          y={str(p.relative_to(work)):h(p) for p in work.rglob('*') if p.is_file()}
          changed={p for p in x.keys()|y.keys() if x.get(p)!=y.get(p)}
          expected={
            'KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_FOUNDATION_REPORT_2026-09-10.md',
            'package.json',
            'scripts/verify-live-crypto-bubble-metric-input-projection-foundation.mjs',
            'src/services/market-data/index.ts',
            'src/services/market-data/liveCryptoBubbleMetricInputProjection.ts',
            'tests/live-crypto-bubble-metric-input-projection-foundation.test.ts',
          }
          assert changed==expected, f'exact scope mismatch: unexpected={sorted(changed-expected)} missing={sorted(expected-changed)}'
          PY

          cd "$RUNNER_TEMP"
          rm -f "$OUT_ZIP"
          (cd "$WORK" && zip -qr "$RUNNER_TEMP/$OUT_ZIP" kairos_p76)
          python - <<'PY'
          from pathlib import Path, PurePosixPath
          import hashlib, os, zipfile
          z=Path(os.environ['RUNNER_TEMP'])/'KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_FOUNDATION_CANDIDATE_2026-09-10.zip'
          data=z.read_bytes()
          with zipfile.ZipFile(z) as a:
            assert a.testzip() is None
            assert {i.filename.split('/')[0] for i in a.infolist()}=={'kairos_p76'}
            for i in a.infolist():
              p=PurePosixPath(i.filename)
              assert not p.is_absolute() and '..' not in p.parts
              assert ((i.external_attr >> 16) & 0o170000) != 0o120000
          print('CANDIDATE_SIZE='+str(len(data)))
          print('CANDIDATE_SHA256='+hashlib.sha256(data).hexdigest())
          PY
          cp "$RUNNER_TEMP/$OUT_ZIP" "$GITHUB_WORKSPACE/$OUT_ZIP"
        env:
          BASE: ${{ runner.temp }}/live-crypto-bubble-metric-input-projection-base
          WORK: ${{ runner.temp }}/live-crypto-bubble-metric-input-projection-work

      - name: Upload verified non-canonical candidate
        uses: actions/upload-artifact@v4
        with:
          name: KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_CANDIDATE
          path: KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_FOUNDATION_CANDIDATE_2026-09-10.zip
          if-no-files-found: error

      - name: Place verified candidate at repository root
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'kairos-autonomous-worker'
          git config user.email 'kairos-autonomous-worker@users.noreply.github.com'
          git add KAIROS_LIVE_CRYPTO_BUBBLE_METRIC_INPUT_PROJECTION_FOUNDATION_CANDIDATE_2026-09-10.zip
          if git diff --cached --quiet; then
            echo 'Candidate already present with identical bytes.'
            exit 0
          fi
          git commit -m 'candidate: add Live Crypto Bubble metric-input projection'
          git push origin HEAD:main
