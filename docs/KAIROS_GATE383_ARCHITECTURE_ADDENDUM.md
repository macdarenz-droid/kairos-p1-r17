# Gate383 canonical promotion and runtime lifecycle stability boundary

## Canonical authority

Kairos Controlled Roadmap Gate #383, run `34588719761`, job `verify-current-candidate` / `103228822544`, exact head `775c5d82bfd8c9623757c2ec6cc1ea30abffedc3`, completed full SUCCESS on 2026-09-11.

Every required canonical stage succeeded: exact uploaded archive identity and integrity, exact Gate382-to-candidate repair scope, deterministic install, exact Lightweight Charts dependency, production TypeScript compilation and build, dedicated lifecycle-stability and released-composition verifiers, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload and gate-evidence upload.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: artifact `10195203836`, wrapper size `1,489,022` bytes, digest `sha256:14cb05505a51c7db9bd8625c661df0b6241074a87a5160e5e71f8c10cffa2191`.
- `KAIROS_GATE_EVIDENCE`: artifact `10195204285`, wrapper size `1,476` bytes, digest `sha256:3e0d9b1f0f841c1ea6d9bd3865b0ee00cb7928e842a83fedcb7d363a3956d0e3`.

Independent exact-run artifact inspection re-proved that the candidate wrapper contains exactly `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_CONFIGURED_RUNTIME_LIFECYCLE_STABILITY_REPAIR_CANDIDATE_2026-09-11.zip`, inner size `1,700,687` bytes, SHA-256 `2204855d3ed2aabf81fcb8502f94c2de8c6527e460e435cfd86f85ffd9dc93f1`, clean ZIP integrity and root exactly `kairos_p76/`. The repository-root file is the same size and Git blob `491b2dbedabdcf8f60f50c716d98fe13900efea5`.

This candidate is now the latest canonical GOLDEN.

## Released ownership

Gate383 repairs only the existing React runtime lifecycle owner in `src/app/useHomeDashboardLiveCryptoBubblePresentationObservedRuntime.ts`.

The effect now restarts for changed effective caller inputs rather than recreated wrapper-object identities: observed/evaluation clock functions, excluded-stablecoin set reference, Top-N count, AbortSignal, optional lifecycle document/timer and neutral-movement threshold. Equivalent configured wrapper recreation no longer resets observation/error evidence. Replacing an effective caller input still performs one cleanup/restart, and late callbacks from superseded runtimes remain ignored.

Readonly collections remain caller-owned and identity-sensitive; in-place mutation is not supported. Gate383 adds no second state/effect/cache/timer, deep comparison, provider semantics, universe policy, radius defaults, viewport measurement, layout/renderer, palette, `HomeRoute` consumer, persistence or Your Trades ownership.

## Next dependency boundary

Fresh Gate383 source preserves a stable configured pixel-radius runtime, but visible Bubble presentation still lacks authoritative product/design policy for default minimum/maximum pixel radii, responsive/container measurement, collision or packing, label fit and hit targets, renderer choice, palette/dimming and motion. Those values must not be invented.

The next worker must first prove from fresh source whether one smaller caller-supplied, default-free presentation contract can be isolated without selecting those semantics. If not, the chain has reached a genuine product/design-authority hold and should request only the missing decisions rather than add hidden defaults.
