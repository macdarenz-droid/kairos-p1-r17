# Kairos Gate 366 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #366 / run `34544282259` / job `103093426159` completed full SUCCESS on 2026-09-11 at exact head `25d78b0178a3dd08b2fc284ddcfe010cc4496cc4`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_REACT_RADIUS_SCALE_HOOK_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-11.zip`

Canonical candidate identity: size `1,703,684` bytes; SHA-256 `7a117e395785cb6abadf28473eb6a600071df509d8613727765bfbca950435b1`; Git blob `c5eba3a94e32e4eeaff47e2c7feb4f674cdb92f2`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10178719912`, wrapper size `1,454,441` bytes, digest `sha256:3378e7cbea66dce4dfd35f79960d676e54c4bb30e0d4e2cdd102522afc08102e`.
- `KAIROS_GATE_EVIDENCE`: id `10178720204`, wrapper size `1,469` bytes, digest `sha256:b976ee7fcfbd91324347fedc9d13761cb04cd9a935613b7194e4a962268de97b`.

Every required canonical stage passed: exact uploaded archive identity/integrity, authoritative Gate365 base/candidate extraction, exact controlled five-file React radius-scale hook-composition scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated current/lower-owner verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload and gate-evidence upload.

## Canonically released responsibility

Gate366 releases `src/app/useHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel.ts` as the thin React composition seam between the released Gate355 area-weight hook path and the released Gate365 radius-scale view-model projector.

`useHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)` owns only this delegation:
1. accept the exact caller-owned `readObservedAt`, `readEvaluationTimeMs`, and existing `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` references;
2. call released `useHomeDashboardLiveCryptoBubbleReactAreaWeightViewModel(...)` exactly once with those exact references;
3. pass the exact returned area-weight view-model reference once to released Gate365 `projectHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)`; and
4. return the exact Gate365 radius-scale view-model result unchanged.

Gate351 remains the React lifecycle/state owner. Gate352 remains normalized area-weight arithmetic owner. Gate354 remains the area-weight view-model owner. Gate355 remains the existing area-weight hook composition owner. Gate364 remains normalized `sqrt(areaWeight)` radius-scale arithmetic owner. Gate365 remains the pure area-weight-view-model-to-radius-scale-view-model owner.

## Explicit non-scope

Gate366 does **not** own or change:
- a second state/effect/memo/cache owner, clocks or timers;
- provider/request/response/acquisition/retry behavior;
- universe eligibility, ranking, Top-30, stablecoin exclusion, tie-break, movement or freshness product policy;
- pixel radius, minimum/maximum visible radius, viewport/device breakpoints, collision/packing/layout, coordinates, labels, interaction or hit targets;
- palette/theme-token selection, CSS, stale/expired opacity, motion or animation;
- `HomeRoute` wiring;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward or Calculation Brain.

Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

## Next dependency-safe responsibility proof

Fresh Gate366 source now exposes a React radius-scale hook, but that hook deliberately still requires the two caller-owned clock sources plus one exact runtime-binding-options object. Gate358 already canonically owns the concrete browser clock sources, and the existing Gate359 browser textual-runtime composition establishes the safe pattern for injecting those browser sources without moving clock ownership into a higher React component. Fresh repository search shows no released browser-specific radius-scale hook currently performs this adjacent composition.

The smallest dependency-safe next responsibility is therefore a **browser React radius-scale hook composition boundary** only:
1. accept one exact caller-owned `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` reference;
2. call Gate366 `useHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)` exactly once;
3. inject Gate358 `readHomeDashboardLiveCryptoBubbleBrowserObservedAt` unchanged as the observation-time source;
4. inject Gate358 `readHomeDashboardLiveCryptoBubbleBrowserEvaluationTimeMs` unchanged as the freshness-evaluation-time source;
5. forward the exact caller-owned options reference unchanged; and
6. return the exact Gate366 radius-scale view-model result unchanged.

This boundary must add no state/effects/memoization, clock implementation, provider/product policy, radius arithmetic, pixel sizing, viewport/collision/layout/labels, palette/CSS/motion, route wiring, persistence or Your Trades behavior. It closes only the already-released browser-clock dependency edge for radius-scale evidence; responsive pixel geometry and visible Bubble rendering remain later controlled presentation responsibilities.