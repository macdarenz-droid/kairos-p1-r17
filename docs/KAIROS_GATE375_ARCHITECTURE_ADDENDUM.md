# Kairos Gate375 Architecture Addendum — Live Crypto Bubble Pixel-Radius Projection

Canonical authority: `Kairos Controlled Roadmap Gate` #375 / run `34564236226`, job `verify-current-candidate` / `103152958825`, exact head `8fc3f1e83610aafefab590650a05d8de892f40c2`, FULL SUCCESS.

Exact promoted candidate: `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_PIXEL_RADIUS_PROJECTION_FOUNDATION_CANDIDATE_2026-09-11.zip`, size `1,738,649` bytes, SHA-256 `4794e12b1ee2a519b5e001e4be5c5b43f23cac332204c174c7260de6a9a06e24`, Git blob `906169460636640cc19c6ec0fcad4a968f0ff034`, root exactly `kairos_p76/`.

Exact-run canonical artifacts:
- `KAIROS_CURRENT_CANDIDATE` artifact `10185682266`, wrapper size `1,483,120` bytes, digest `sha256:ffd6c85e09a712fd75bc2cdb1c1299df5429ea0402d4df417a5dc04d0f5807db`.
- `KAIROS_GATE_EVIDENCE` artifact `10185682541`, wrapper size `1,743` bytes, digest `sha256:20bed2744355390408948e12d3d49a2783d2949ccec86a53fd1c73d0ce7a3a11`.

Every canonical stage succeeded, including exact archive identity/integrity, exact five-file Gate374→Gate375 scope with zero removals, deterministic install, exact Lightweight Charts 5.2.1, production TypeScript/build, dedicated pixel-radius projection verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload, and gate-evidence upload.

## Canonically released responsibility

Gate375 releases `src/application/dashboard/homeDashboardLiveCryptoBubblePixelRadiusProjection.ts` as a pure provider-neutral projection boundary over already-released normalized radius-scale evidence and caller-supplied Gate374 pixel-radius policy.

The released projection owns only deterministic mapping of a present normalized `radiusScale` inside exact caller-supplied validated bounds. It preserves upstream failure/null/order/reference semantics, maps `radiusScale: null` to `radiusCssPixels: null`, validates present normalized radius-scale evidence in `[0,1]`, and computes only:

`minimumRadiusCssPixels + radiusScale * (maximumRadiusCssPixels - minimumRadiusCssPixels)`

It does not clamp, sort, filter, invent fallback values, or choose product/design defaults.

## Ownership retained below and above Gate375

- Gate374 remains the caller-owned pixel-radius bounds contract/validator.
- Gate373 remains the HomeRoute normalized-radius textual-runtime integration owner.
- Gate372 remains the configured-browser normalized-radius textual runtime owner.
- Gate371 remains the normalized-radius textual evidence presenter owner.
- Gate369 remains the configured-browser normalized-radius hook owner.
- Gate364 and lower released owners retain normalized radius/area/market-truth projection and arithmetic.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

Gate375 does not move route, renderer, layout, styling, market truth, acquisition, freshness, persistence, or Your Trades authority into the projection.

## Explicit non-scope retained

Gate375 does **not** own or choose:
- concrete/default minimum or maximum Bubble radii;
- responsive breakpoints or browser viewport measurement;
- collision detection, packing, layout, label-fit, or hit-target policy;
- SVG/canvas/DOM renderer choice;
- Bubble palette, CSS, theme, dimming strength, or motion;
- HomeRoute mutation or a second Live Crypto runtime consumer;
- Binance/provider transport, acquisition, retries, cadence, freshness, universe, ranking, Top-N, stablecoin exclusion, movement, or market-truth semantics;
- persistence, Saved Analysis, Your Trades, journal, chart, Risk/Reward, or Calculation Brain truth.

No concrete visual sizing value is established by this canonical PASS. Those values remain caller/product/design-owned and must not be guessed.

## Next dependency boundary

Fresh Gate375 source now canonically owns the deterministic numeric conversion from released normalized radius evidence plus caller-supplied bounds to optional CSS-pixel radius evidence. The existing presentation chain already demonstrates a separate pure React text-evidence boundary for normalized radius values at Gate371, while Gate375 intentionally owns no React, route, runtime, or default policy.

Therefore the next smallest dependency-safe technical responsibility may be one **pure pixel-radius text-evidence presentation boundary** over Gate375 output only: accept exact caller-produced pixel-radius projection evidence, preserve upstream failure/null/order/instrument association, render only the released optional `radiusCssPixels` evidence, and keep `null` visibly missing. It must not create or choose radius bounds, call browser viewport APIs, own hooks/state/effects, introduce collision/packing/layout/labels, mutate HomeRoute, or become a renderer.

A configured runtime or visible Bubble renderer still requires authoritative caller/product/design sizing inputs before it can be wired into HomeRoute; those values remain unresolved and must not be guessed.
