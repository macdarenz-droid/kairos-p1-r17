# Kairos Gate 352 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #352 / run `34470528673` / job `102849110869` completed full SUCCESS on 2026-09-10 at exact head `2aa5cfce054ceb272ca15db4340eb19ea127c491`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_AREA_WEIGHT_PROJECTION_FOUNDATION_CANDIDATE_2026-09-10.zip`

Canonical candidate identity: size `1,636,871` bytes; SHA-256 `f899195a8f61c2a817e1c2f4b2d02cf20ec662df8a9fc81631a6f74d98aea1fc`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10149687875`, wrapper size `1,400,614` bytes, digest `sha256:a1fb014fd90fb707aceb52792a2e1787e685d11bbb327f255338d88df5e43999`.
- `KAIROS_GATE_EVIDENCE`: id `10149688323`, wrapper size `1,388` bytes, digest `sha256:d37b7d35d38ae845b7664663a61137492aa2a86a54c4c0ee8a40191f3ec22257`.

The exact-run current-candidate artifact was independently downloaded after the run. Its wrapper contained exactly one nested candidate with the exact filename, size, SHA-256 and root above; ZIP integrity was clean. Every required canonical stage passed: exact archive identity/integrity, Gate351 base/candidate extraction, exact five-file scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript/build, dedicated area-weight verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both canonical uploads.

## Canonically released responsibility

Gate352 releases `src/application/dashboard/homeDashboardLiveCryptoBubbleAreaWeightProjection.ts` as the provider-neutral normalized visual-area preparation boundary for the Home Dashboard Live Crypto Bubble Map.

`projectHomeDashboardLiveCryptoBubbleAreaWeights(...)` owns only this deterministic projection:

1. accept one exact Gate346 presentation-state projection result without changing entry order or upstream identity;
2. preserve an upstream projection failure as exact non-renderable evidence;
3. inspect only released non-negative `quoteVolume24h` values for `availability: 'present'` entries;
4. derive `areaWeight = quoteVolume24h / maximum present quoteVolume24h` through the released Decimal kernel;
5. preserve each exact upstream presentation entry by reference;
6. keep missing entries explicit with `areaWeight: null`; and
7. when every present quote volume is zero, return exact Decimal zero for every present area weight rather than inventing a minimum visible size.

The authoritative Bubble-size fact remains 24h quote volume. `areaWeight` is only a normalized presentation input and never replaces the metric truth.

## Explicit non-scope

Gate352 does **not** own or change:
- sorting, filtering, ranking, Top-N selection or stablecoin-exclusion policy;
- movement/freshness derivation, observed-at/evaluation-time ownership, acquisition cadence or provider/runtime/session truth;
- pixel radius, square-root conversion, minimum/maximum radius, viewport geometry or device sizing;
- collision/layout, label placement, interactions or animation;
- design-system palette/tokens, CSS classes, opacity/dimming strength or accessibility copy;
- DOM/React lifecycle, `HomeRoute` wiring or visible Bubble circles;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, Calculation Brain, navigation or transitions.

## Next dependency-safe responsibility proof

Fresh Gate352 GOLDEN source now provides an exact provider-neutral `areaWeight` result, while Gate351 exposes React-local runtime state whose `latestObservation` preserves the exact Gate349 semantic observation and separate lifecycle/error evidence. `src/app/HomeRoute.tsx` remains an empty dashboard foundation and current design-system tokens contain no Bubble-specific pixel-radius or Bubble-specific palette contract.

Directly choosing circle radius, minimum visible radius, collision geometry or concrete colors at this point would therefore invent a user-facing presentation rule that no released owner currently defines. Widening Gate349 or Gate351 to embed area weights would also retroactively alter already-released responsibilities.

The smallest dependency-safe next responsibility is instead a **Home Live Crypto Bubble React area-weight view-model projection foundation** below visible rendering:

1. accept one exact Gate351 React runtime-state value and preserve that exact state by reference;
2. when `latestObservation` is `null`, expose no area-weight projection rather than inventing market data;
3. when an observation exists, pass only its exact `presentationStateProjection` into the released Gate352 projection exactly once;
4. expose the exact Gate352 result separately from Gate351 lifecycle/error state so acquisition/bootstrap errors never erase the last authoritative semantic observation or its derived area-weight evidence;
5. perform no clocks, provider calls, acquisition, retries, policy selection, sorting/filtering, movement/freshness reclassification, pixel-radius conversion, CSS/theme selection, geometry/layout, DOM/React rendering, persistence or Your Trades/journal work; and
6. keep this as a pure app/view-model composition seam so a later controlled renderer can consume released state without becoming a hidden market/business owner.

This step is reversible and introduces no new user-facing radius, layout or palette decision. It closes the dependency seam between released React runtime state and released normalized area-weight evidence before any visible Bubble renderer is allowed to choose geometry/design semantics.