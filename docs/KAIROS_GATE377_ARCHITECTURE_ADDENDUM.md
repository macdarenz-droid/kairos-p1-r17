# Kairos Gate377 Architecture Addendum — Live Crypto Bubble React Pixel-Radius View Model

Canonical authority: `Kairos Controlled Roadmap Gate` #377 / run `34570762112`, job `verify-current-candidate` / `103172156724`, exact head `e6b9a59705c29677ace68403ab236dfb5c4a99c3`, FULL SUCCESS.

Exact promoted candidate: `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_REACT_PIXEL_RADIUS_VIEW_MODEL_FOUNDATION_CANDIDATE_2026-09-11.zip`, size `1,749,453` bytes, SHA-256 `581d490805be56e11e8fa45815e52a9a1ba4b58da0a44fa2a830cc511a0ef5f6`, Git blob `8e65c433e1c812e29e07cf6ad5b248a91d1ec295`, root exactly `kairos_p76/`.

Exact-run canonical artifacts:
- `KAIROS_CURRENT_CANDIDATE` artifact `10187976674`, wrapper size `1,491,476` bytes, digest `sha256:c44ee68ea6bab538e75a3920592d8e431ae71c17261037e1ec16f276e12ddef6`.
- `KAIROS_GATE_EVIDENCE` artifact `10187977094`, wrapper size `1,227` bytes, digest `sha256:7f72ff18df1e288fc673842ed050c6e3aa0c44fb8586a32834e5ebcf21e8fab5`.

Every canonical stage succeeded, including exact archive identity/integrity, exact five-file Gate376→Gate377 scope with zero removals, deterministic install, exact Lightweight Charts 5.2.1, production TypeScript/build, dedicated React pixel-radius view-model verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload, and gate-evidence upload.

## Canonically released responsibility

Gate377 releases `src/app/homeDashboardLiveCryptoBubbleReactPixelRadiusViewModel.ts` as the pure app-level composition from the released Gate369 React normalized-radius view model to the released Gate375 CSS-pixel radius projection under an exact caller-supplied Gate374 pixel-radius policy.

`projectHomeDashboardLiveCryptoBubbleReactPixelRadiusViewModel(...)`:
- accepts the exact released `HomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel` reference and exact caller policy;
- preserves the exact radius-scale view-model and `runtimeState` references;
- keeps `pixelRadiusProjection` exactly `null` while `radiusScaleProjection` is `null`;
- otherwise delegates exactly once to `projectHomeDashboardLiveCryptoBubblePixelRadii(...)` with the exact released radius-scale projection reference and exact caller policy;
- returns the resulting released Gate375 projection without inventing fallback sizing or a second runtime state owner.

## Ownership retained below and above Gate377

- Gate376 remains the pure React pixel-radius text-evidence presenter owner.
- Gate375 remains the provider-neutral CSS-pixel radius projection owner.
- Gate374 remains the caller-owned pixel-radius bounds contract/validator.
- Gate369 remains the pure React normalized-radius view-model owner.
- Gate366 remains the thin React normalized-radius hook-composition owner over the released area-weight hook and Gate365 projector.
- Gate351 and lower released owners retain the single React runtime lifecycle/state chain, market/provider, acquisition, freshness, universe, movement and observation responsibilities.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

Gate377 moves no hook/state/effect, clock, browser viewport, renderer, layout, style, route, provider, persistence or Your Trades authority into the view model.

## Explicit non-scope retained

Gate377 does **not** own or choose:
- concrete/default minimum or maximum Bubble radii;
- responsive breakpoints or browser viewport measurement;
- collision detection, packing, layout, label-fit or hit-target policy;
- SVG/canvas/DOM renderer choice;
- Bubble palette, CSS, theme, dimming strength or motion;
- `HomeRoute` wiring or a second Live Crypto runtime consumer;
- Binance/provider transport, acquisition, retries, cadence, freshness, universe, ranking, Top-N, stablecoin exclusion, movement or market-truth semantics;
- persistence, Saved Analysis, Your Trades, journal, chart, Risk/Reward or Calculation Brain truth.

No concrete visual sizing value is established by this canonical PASS. Those values remain caller/product/design-owned and must not be guessed.

## Next dependency boundary

Fresh Gate377 source now provides the pure app-level pixel-radius view model, while the released Gate366 normalized-radius chain already proves the matching thin React hook-composition pattern: caller-owned runtime inputs are passed unchanged into the released normalized-radius hook, and the exact returned view model is delegated once into a pure app-level projector.

The smallest dependency-safe technical continuation is therefore a **pure React pixel-radius hook-composition boundary**: accept the exact caller-owned observed-at source, freshness evaluation-time source, Gate351 runtime-binding options and exact caller-supplied Gate374 pixel-radius policy; call `useHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)` exactly once with the exact runtime references; pass the exact returned radius-scale view model plus exact caller policy exactly once to `projectHomeDashboardLiveCryptoBubbleReactPixelRadiusViewModel(...)`; and return the exact Gate377 result unchanged.

That hook must own no second state/effect/cache/timer, no clock or viewport API, no default radius values, no policy arithmetic/validation, no collision/packing/layout/renderer/styling, no `HomeRoute` wiring, no provider/market semantics, no persistence and no Your Trades truth. Browser/configured-runtime or visible Bubble rendering work remains later and still requires caller/product/design sizing inputs; Gate377 does not authorize inventing those values.
