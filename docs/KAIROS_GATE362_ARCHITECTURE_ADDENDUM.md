# Kairos Gate 362 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #362 / run `34532199762` / job `103055358176` completed full SUCCESS on 2026-09-10 at exact head `b571f1b9845013231b56ac7bad53f9e971d3c5ac`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_RUNTIME_PRODUCT_POLICY_FOUNDATION_CANDIDATE_2026-09-11.zip`

Canonical candidate identity: size `1,681,458` bytes; SHA-256 `ed812610da32a163627e77f539f749a54f0a09f316010208e3d290e113a1475b`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10174356089`, wrapper size `1,436,731` bytes, digest `sha256:98f6c225f22eebb1272f0d6c4abbcefc53b84e00eee7cdd28774c9e214a353ed`.
- `KAIROS_GATE_EVIDENCE`: id `10174356708`, wrapper size `1,743` bytes, digest `sha256:4742370e067e955895b9469d5e2569b749b7a45ed0d5bde49cf41f1c0820d6df`.

Every required canonical stage passed: exact archive identity/integrity, authoritative Gate361 base/candidate extraction, exact five-file runtime-product-policy scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both canonical uploads.

Independent exact-run artifact inspection re-proved the nested candidate filename, exact size and SHA-256, root exactly `kairos_p76/`, clean ZIP integrity, and no absolute paths, traversal paths, or symlinks.

## Canonically released responsibility

Gate362 releases `src/app/homeDashboardLiveCryptoBubbleRuntimeProductPolicy.ts` as the single default runtime product-policy owner for the user-approved Home Live Crypto Bubble V1 configuration values consumed by Gate360.

It owns only:
1. the exact configurable default excluded-stablecoin base-asset set `USDC`, `FDUSD`, `XUSD`, `USDS`, `U`, `TUSD`, `USDP`, `DAI`, `EURI`, `AEUR`;
2. the exact neutral absolute 24h movement threshold `0.25` percent;
3. keeping `USDT` available as the required quote asset and not excluding it;
4. not classifying `PAXG` as a stablecoin; and
5. constructing a fresh `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration` with a fresh `Set` for callers choosing the approved defaults.

Gate360 remains runtime-options composition owner. Gate361 remains configured browser textual-runtime composition owner. Existing universe/ranking, Top-30, provider/acquisition, lifecycle/cadence, freshness, clocks, React state, semantic presentation and area-weight owners remain unchanged.

## Explicit non-scope

Gate362 does **not** own or change:
- `HomeRoute` or router wiring;
- visible Bubble radius/pixel scaling, square-root conversion, minimum/maximum radius, viewport/device geometry, collision/layout, label placement, SVG/canvas or interaction;
- concrete theme token values, Bubble palette, CSS, dimming intensity or motion;
- provider/request/response/acquisition/retry behavior;
- Top-N count, ranking or symbol tie-break semantics;
- freshness/cadence/lifecycle/timer/clock behavior;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, Calculation Brain, navigation or transitions.

The separately approved renderer policy remains: authoritative metric truth is 24h quote volume; released `areaWeight` is visual-area evidence; circle area is proportional to `areaWeight`, therefore radius is proportional to `sqrt(areaWeight)`; renderer-owned responsive min/max radius, collision/layout and label fit remain presentation concerns; semantic colours must consume Kairos theme/design tokens; stale remains visibly dimmed/retained and expired must not masquerade as current.

## Next dependency-safe responsibility proof

Fresh Gate362 GOLDEN now resolves the concrete product values that Gate361 previously required from its caller. Gate361 is explicitly the configured browser textual-runtime edge immediately below eventual `HomeRoute` integration, and `src/app/HomeRoute.tsx` remains a static placeholder.

The existing released chain therefore provides every non-visual dependency needed to connect truthful Live Crypto Bubble textual evidence to the Home route without choosing any new market, provider, freshness, ranking, stablecoin, neutral-threshold, radius, layout, palette or persistence semantics.

The smallest dependency-safe next responsibility is **Home route textual-runtime integration only**:
1. `HomeRoute` chooses the released Gate362 default product configuration through its factory rather than duplicating values;
2. it passes that exact configuration to released Gate361 `HomeDashboardLiveCryptoBubbleConfiguredTextEvidenceRuntime`;
3. it replaces the current disconnected-dashboard placeholder with the already-released truthful textual evidence path;
4. it introduces no Bubble geometry, styling, radius, layout, theme-token mapping, provider behavior, business/data calculation, persistence or Your Trades logic; and
5. it keeps later visible Bubble rendering as a separate presentation slice under the already-approved renderer policy.

This route integration is safe, reversible and dependency-complete. It makes the released Live Crypto runtime observable from the Home route while preserving one owner per product/data/presentation responsibility and avoids turning `HomeRoute` into a hidden policy or rendering owner.