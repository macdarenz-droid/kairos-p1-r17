# Kairos Gate 345 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #345 / run `34439420490` / job `102751141072` completed full SUCCESS on 2026-09-10 at exact head `a18451a365202e282f2694e55960d725c9fd0217`.

Exact canonical candidate:
`KAIROS_BINANCE_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_OBSERVED_RUNTIME_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-10.zip`

Canonical candidate identity: size `1,606,330` bytes; SHA-256 `3cb6852aba0d5a2037f4e134254d2852585d3186acf5a6695c538323685ede49`; root exactly `kairos_p76/`; repository-root Git blob `c456123ec3ea99d7e164a1a1d58a9cdc6592b19a`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10137567107`, wrapper size `1,374,746` bytes, digest `sha256:6ec0733ea07f9e78159fbe0ddad27a3c192c5cde819ada1605cb0e7094a0ec44`.
- `KAIROS_GATE_EVIDENCE`: id `10137567531`, wrapper size `1,552` bytes, digest `sha256:c5f8b91a0a293fd1a5134bd1d6b2eba0a11f550258eb39b138532c514692e94d`.

The exact-run `KAIROS_CURRENT_CANDIDATE` artifact was independently downloaded after the run. Its wrapper contained exactly the candidate above; nested candidate size, SHA-256 and root were re-proved exactly.

Every required canonical stage passed: setup/checkout, pinned npm verification, exact archive identity/integrity, authoritative Gate344 base plus candidate extraction, exact five-file Binance Home Bubble observed-runtime composition scope, deterministic install, exact Lightweight Charts dependency proof, production TypeScript compilation, production build, dedicated current-slice verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both exact-run canonical artifact uploads.

## Canonically released responsibility

Gate345 releases `src/app/binanceHomeDashboardLiveCryptoBubbleObservedRuntimeComposition.ts` as the app-level Binance Home Live Crypto Bubble observed-runtime composition below React presentation.

`startBinanceHomeDashboardLiveCryptoBubbleObservedRuntime(...)` owns only this composition:

1. preserve the caller-owned acquisition `readObservedAt` source;
2. preserve the caller-owned freshness `readEvaluationTimeMs` source;
3. preserve Gate341 universe and browser lifecycle options unchanged;
4. replace Gate341's raw freshness-observation sink at this wrapper boundary with the released Gate344 `HomeDashboardLiveCryptoBubbleMetricObservationSink` type;
5. construct exactly one Gate344 adapter from that Bubble metric sink;
6. pass that adapter unchanged as Gate341's sole raw `observationSink`;
7. delegate exactly once to `startBinanceHomeDashboardLiveMarketObservedRuntime(...)`; and
8. return Gate341's runtime bootstrap result unchanged.

Initial-universe failure, authoritative-empty-scope behavior, non-empty lifecycle cadence/visibility, and close semantics remain lower-owner runtime truth. Caller time sources are passed by reference and are not invoked by Gate345 itself.

## Explicit non-scope

Gate345 does **not** own or change:
- Binance/provider transport or response mapping;
- active-USDT selection, stablecoin exclusion, quote-volume ranking, symbol tie-break or Top-N policy;
- acquisition cadence, browser visibility lifecycle, retry/backoff, universe refresh, session mutation or close semantics;
- observedAt/evaluation-time generation, internal clocks, freshness thresholds or freshness classification;
- Bubble palette, sign-to-color presentation, neutral threshold, radius scaling, geometry/collision/layout, labels/interactions/animation, or stale/expired/missing visual treatment;
- persistence/IndexedDB/Saved Analysis;
- React/Home route state or rendering;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, Calculation Brain, navigation or transitions.

## Next dependency-safe responsibility proof

Gate345 closes the final currently-proven non-presentation composition seam between the Binance Home runtime and the released Bubble metric observation stream. Fresh source still shows no production consumer of `startBinanceHomeDashboardLiveCryptoBubbleObservedRuntime(...)`; `HomeRoute` remains the P21.1 presentation owner with an explicit "No dashboard insights are connected yet" empty state.

The controlling P21 product contract already fixes several presentation semantics without fixing visual styling details:
- Bubble size truth is exact 24h quote volume, while nonlinear radius scaling is presentation-owned.
- Bubble color meaning is 24h percentage movement: positive gain, negative loss, near-zero neutral; actual palette remains design-system-owned.
- FRESH / STALE / EXPIRED remain explicit freshness truth; stale retains the last authoritative facts visibly stale/dimmed; expired must not masquerade as current.
- Important visual information must not rely on color alone.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate truth domains.

Therefore the next smallest dependency-safe responsibility is **not** direct Bubble geometry or a full Home component. It is a provider-neutral **Home Live Crypto Bubble presentation-state projection contract** that converts one released Gate344 Bubble metric observation into deterministic semantic presentation state while preserving underlying metric/freshness truth.

Before candidate construction, freeze this boundary from existing presentation conventions. The safe target is to classify only already-approved semantic states needed by React—present/missing metric availability, positive/negative/near-zero movement category, and fresh/stale/expired status—while retaining exact numeric metric values and source associations. It must not choose CSS/token values, radius math, coordinates/collision/layout, DOM structure, animation, or start/stop runtime lifecycle. Any near-zero boundary must come from an explicit caller/config policy rather than be invented in UI code. Expired data must remain explicitly non-current rather than silently appearing fresh.

A later React/Home slice may consume that semantic projection and own component state/rendering; geometry/layout and design-token styling remain separate presentation responsibilities.