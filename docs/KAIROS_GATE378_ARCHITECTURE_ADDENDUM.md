# Kairos Gate378 Architecture Addendum — Live Crypto Bubble React Pixel-Radius Hook Composition

Canonical authority: `Kairos Controlled Roadmap Gate` #378 / run `34574062778`, job `verify-current-candidate` / `103182442413`, exact head `855954f88b873b5839761a347598e408c23d2032`, FULL SUCCESS.

Exact promoted candidate: `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_REACT_PIXEL_RADIUS_HOOK_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-11.zip`, size `1,755,402` bytes, SHA-256 `5590d98e7b4beafc594e226bbd97254e825fa130a8e05081042388a626a967ee`, Git blob `a521d96e99b39881da4b7220ef2a92280218191e`, root exactly `kairos_p76/`.

Exact-run canonical artifacts:
- `KAIROS_CURRENT_CANDIDATE` artifact `10189250446`, wrapper size `1,496,702` bytes, digest `sha256:e0d020b3c0494566a161861f9ee12617a11633d88f64244bb6cba9c94abb8940`.
- `KAIROS_GATE_EVIDENCE` artifact `10189250751`, wrapper size `1,578` bytes, digest `sha256:c3a56e9fea7509bd3b7b918966ff38e0acbfd10b592558f95f688d10ab15d74e`.

Every canonical stage succeeded, including exact archive identity/integrity, exact five-file Gate377→Gate378 scope with zero removals, deterministic install, exact Lightweight Charts 5.2.1, production TypeScript/build, dedicated React pixel-radius hook-composition verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload, and gate-evidence upload. Independent extraction of the exact-run `KAIROS_CURRENT_CANDIDATE` artifact re-proved the nested candidate byte identity above.

## Canonically released responsibility

Gate378 releases `src/app/useHomeDashboardLiveCryptoBubbleReactPixelRadiusViewModel.ts` as the thin React composition edge from the released Gate366 normalized-radius hook chain to the released Gate377 pixel-radius view-model projector.

`useHomeDashboardLiveCryptoBubbleReactPixelRadiusViewModel(...)`:
- accepts the exact caller-owned acquisition observed-at source, freshness evaluation-time source, Gate351 runtime-binding options, and exact caller-supplied Gate374 pixel-radius policy;
- calls `useHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)` exactly once with the exact caller runtime references;
- passes the exact returned normalized-radius view-model reference plus exact caller policy exactly once to `projectHomeDashboardLiveCryptoBubbleReactPixelRadiusViewModel(...)`;
- returns the exact Gate377 pixel-radius view-model result unchanged.

## Ownership retained below and above Gate378

- Gate377 remains the pure normalized-radius-view-model-to-pixel-radius-view-model owner.
- Gate376 remains the pure pixel-radius textual-evidence presenter owner.
- Gate375 remains the provider-neutral CSS-pixel projection owner.
- Gate374 remains the caller-owned pixel-radius policy contract/validator.
- Gate369 remains the pure normalized-radius React view-model owner.
- Gate366 and lower released owners retain the single React runtime lifecycle/state chain, market/provider acquisition, freshness, universe, movement and observation responsibilities.
- Gate358 remains the concrete browser wall-clock source owner.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

Gate378 adds no second state/effect/cache/timer owner, no local clock read, no browser viewport source, no policy arithmetic or validation, no default CSS-pixel sizing, no layout/renderer/style/route ownership, no persistence and no Your Trades truth.

## Explicit non-scope retained

Gate378 does **not** own or choose:
- concrete/default minimum or maximum Bubble radii;
- responsive breakpoints or browser viewport measurement;
- collision detection, packing, layout, label-fit or hit-target policy;
- SVG/canvas/DOM renderer choice;
- Bubble palette, CSS, theme, dimming strength or motion;
- `HomeRoute` wiring or a second Live Crypto runtime consumer;
- Binance/provider transport, acquisition, retries, cadence, freshness, universe, ranking, Top-N, stablecoin exclusion, movement or market-truth semantics;
- persistence, Saved Analysis, Your Trades, journal, chart, Risk/Reward or Calculation Brain truth.

No concrete visual sizing value is established by this canonical PASS. Those values remain caller/product/design-owned and must not be guessed.

## Next dependency boundary

Fresh Gate378 source now provides the generic React pixel-radius hook composition, while the released Gate368 browser normalized-radius hook already proves the matching next dependency edge: inject only the released Gate358 browser observed-at and freshness-evaluation-time sources, forward the exact caller runtime options, and return the exact lower hook result without adding a second runtime owner.

The smallest dependency-safe technical continuation is therefore a **browser pixel-radius hook-composition boundary**: accept the exact caller-owned Gate351 runtime-binding options plus exact caller-supplied Gate374 pixel-radius policy; call `useHomeDashboardLiveCryptoBubbleReactPixelRadiusViewModel(...)` exactly once with `readHomeDashboardLiveCryptoBubbleBrowserObservedAt`, `readHomeDashboardLiveCryptoBubbleBrowserEvaluationTimeMs`, the exact caller options reference and exact caller policy reference; and return the exact Gate378 result unchanged.

That browser seam must own no clock implementation, no state/effect/cache/timer, no viewport read, no default radius values, no policy arithmetic/validation, no collision/packing/layout/renderer/styling, no `HomeRoute` wiring, no provider/market semantics, no persistence and no Your Trades truth. Configured-runtime composition and visible Bubble rendering remain later responsibilities; Gate378 does not authorize inventing concrete sizing values.
