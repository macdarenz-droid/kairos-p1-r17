# Kairos Gate 359 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #359 / run `34503359548` / job `102959976240` completed full SUCCESS on 2026-09-10 at exact head `beef74c61a736b91cff67dc925fc7f9f678cd8db`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_BROWSER_TEXT_EVIDENCE_RUNTIME_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-10.zip`

Canonical candidate identity: size `1,667,079` bytes; SHA-256 `914f41cafc98cc56adc046e0aa086103bb37873bb3154fdb5589c308e077c61a`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10163315671`, wrapper size `1,424,961` bytes, digest `sha256:d854b76c5c79c40ae9b8afd3c649b0356e6f5f8f9cadf5be460027fd3d5c5751`.
- `KAIROS_GATE_EVIDENCE`: id `10163316118`, wrapper size `1,332` bytes, digest `sha256:2534ae350e2465ee1cf4cf5fb82108e81fb2cf007920a28cf50a972f7e7b0c53`.

Every required canonical stage passed: exact archive identity/integrity, Gate358 base/candidate extraction, exact five-file scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated browser text-evidence runtime-composition verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both canonical uploads.

Independent exact-run artifact inspection re-proved the nested candidate filename/size/SHA, root exactly `kairos_p76/`, clean ZIP integrity, and no absolute paths, traversal paths, or symlinks.

## Canonically released responsibility

Gate359 releases `src/app/HomeDashboardLiveCryptoBubbleBrowserTextEvidenceRuntime.tsx` as the browser composition edge between the Gate358 concrete browser wall-clock sources and the Gate357 textual-evidence runtime component.

It owns only this delegation:
1. accept one exact caller-owned `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` value;
2. render `HomeDashboardLiveCryptoBubbleTextEvidenceRuntime` exactly once;
3. inject `readHomeDashboardLiveCryptoBubbleBrowserObservedAt` unchanged as `readObservedAt`;
4. inject `readHomeDashboardLiveCryptoBubbleBrowserEvaluationTimeMs` unchanged as `readEvaluationTimeMs`; and
5. forward the exact caller-owned options reference unchanged.

Gate351 retains React lifecycle/state ownership. Gate352 retains normalized area-weight arithmetic. Gate354 retains pure runtime-state-to-area-weight view-model ownership. Gate355 retains hook composition. Gate356 retains semantic textual presentation. Gate357 retains injected runtime-to-presenter composition. Gate358 retains concrete browser wall-clock ownership.

## Explicit non-scope

Gate359 does **not** own or change:
- React state/effects/memo/callback ownership beyond rendering the released child component;
- clocks, timers, animation frames, provider transport, acquisition/retry/cadence, page visibility, freshness thresholds, or state-session truth;
- universe eligibility semantics, the concrete excluded-stablecoin set, quote-volume ordering, symbol tie-break, or Top-N policy;
- neutral-movement threshold configuration, design-system palette/tokens, stale/expired visual treatment, or other presentation-policy defaults;
- Bubble radius/pixel scaling, collision/layout, label placement, SVG/canvas, interaction, CSS, motion, or transitions;
- `HomeRoute` or router wiring;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, or Calculation Brain.

The authoritative Bubble-size metric remains 24h quote volume. Gate359 adds no geometry and no product-policy value.

## Next dependency-safe responsibility proof

Fresh Gate359 GOLDEN closes the browser-clock-to-textual-runtime composition edge, but production `HomeRoute` still cannot safely instantiate the released runtime because the required runtime options contain caller-owned product configuration that has not been canonically assigned concrete application values.

Fresh Gate359 source tracing establishes that:
- `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` still requires `universe` and `presentationPolicy` inputs;
- `LiveMarketUniverseInstrumentEligibilityPolicyOptions` requires an explicit `excludedStablecoinBaseAssets: ReadonlySet<string>` value;
- the released Top-N policy already owns the default count of 30 when `topNCount` is omitted, so a new Top-N default owner is unnecessary;
- the released browser lifecycle adapter already owns browser document/timer defaults and the released cadence policy owns the visible 5-second interval, so a new lifecycle-default owner is unnecessary;
- `HomeDashboardLiveCryptoBubblePresentationPolicy` still requires `neutralMaxAbsoluteMovementPercent` as a caller-provided Decimal value;
- neither the exact default excluded-stablecoin set nor the exact neutral movement threshold has been released by current Kairos authority; and
- both values materially affect user-visible product meaning: universe membership and positive/negative/neutral movement classification.

The smallest safe **technical** responsibility that can proceed without guessing either product value is therefore a pure **Home Live Crypto Bubble runtime-options composition boundary** below `HomeRoute`:
1. accept the excluded-stablecoin set as an exact caller-owned reference;
2. accept the neutral movement threshold as an exact caller-owned Decimal value;
3. construct the existing `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` shape from those exact inputs;
4. deliberately omit `topNCount` so the already-released Top-30 default remains the sole owner;
5. deliberately omit lifecycle overrides so the already-released browser lifecycle/cadence defaults remain the sole owners; and
6. introduce no new product values, provider behavior, clocks, state, geometry, route wiring, persistence, or Your Trades ownership.

That boundary would reduce future `HomeRoute` wiring to supplying two still-explicit product configuration values without duplicating already-released defaults. The actual default excluded-stablecoin contents and neutral-movement threshold remain material product/design decisions and must not be invented by this technical boundary.