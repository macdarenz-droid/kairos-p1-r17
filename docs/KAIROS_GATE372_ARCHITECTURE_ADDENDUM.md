# Kairos Gate372 Architecture Addendum

Date: 2026-09-11

## Canonical authority

`Kairos Controlled Roadmap Gate` #372, run `34556601218`, job `verify-current-candidate` / `103130481445`, completed FULL SUCCESS on head `4651fb0a1aa06978310824b1443467fcd50c0592`.

Every required canonical stage succeeded: exact uploaded archive identity/integrity; Gate371 base/candidate extraction; exact five-file configured-browser radius-scale text-evidence runtime scope; deterministic install; exact Lightweight Charts 5.2.1; production TypeScript/build; dedicated current/lower-owner verification; focused regression; full unit regression; full controlled-roadmap regression; historical closures; candidate upload; and gate-evidence upload.

The exact canonical candidate is `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_CONFIGURED_BROWSER_RADIUS_SCALE_TEXT_EVIDENCE_RUNTIME_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-11.zip`, size `1724390`, SHA-256 `4189c15eb44908c46a1010108c873b838939cfc24322525c3b8b06e99983160a`, Git blob `54fc27bd0d6edbb0bfae25b4433185a3fe17776f`, root exactly `kairos_p76/`.

Exact-run canonical artifacts:
- `KAIROS_CURRENT_CANDIDATE` artifact id `10183042105`, wrapper size `1471033`, wrapper digest `sha256:280cb0b9c9000c0e839ebd8099387dffbf70e7c676e60d26fb54e1497c8e00c2`.
- `KAIROS_GATE_EVIDENCE` artifact id `10183042507`, wrapper size `1546`, wrapper digest `sha256:d6fc64f127bdf21e50444da8c33f94db44844e071ec588a96ff502346f8fa7bc`.

Independent exact-run extraction re-proved the sole nested candidate filename, exact size/SHA/Git blob, `kairos_p76/` root, and no absolute paths, traversal entries, or symlinks.

## Released ownership

Gate372 canonically releases `HomeDashboardLiveCryptoBubbleConfiguredBrowserRadiusScaleTextEvidenceRuntime` as a pure configured browser React composition boundary.

It owns only this exact edge:
1. accept one caller-owned `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration`;
2. call released Gate369 `useHomeDashboardLiveCryptoBubbleConfiguredBrowserRadiusScaleViewModel(configuration)` exactly once;
3. pass the exact returned view model once to released Gate371 `HomeDashboardLiveCryptoBubbleRadiusScaleTextEvidence`;
4. return that presenter unchanged.

It does **not** own additional state/effects/memo/cache; browser clocks/timers/cadence; provider/request/acquisition or product policy; radius arithmetic; pixel radius/min/max/breakpoints; viewport/collision/packing/layout/label-fit; palette/CSS/theme/motion; `HomeRoute` wiring; persistence/IndexedDB/Saved Analysis; or Your Trades/journal/chart/Risk-Reward truth.

## Dependency boundary after Gate372

Fresh Gate372 GOLDEN source shows the complete configured browser normalized-radius textual runtime now exists, but `HomeRoute` still imports and renders the older `HomeDashboardLiveCryptoBubbleConfiguredTextEvidenceRuntime`, whose presentation path is the earlier area-weight textual evidence chain.

Therefore the next smallest dependency-safe responsibility is a controlled `HomeRoute` presentation integration amendment only: keep the existing default product-configuration owner, replace the old configured textual-runtime delegation with exactly one `HomeDashboardLiveCryptoBubbleConfiguredBrowserRadiusScaleTextEvidenceRuntime` delegation, and preserve route ownership and all non-presentation boundaries.

This route amendment must remain presentation-only and must not introduce provider transport, market/product policy, clocks/timers, arithmetic, pixel-radius values, viewport/collision/packing/layout policy, palette/CSS/theme/motion, persistence, or Your Trades truth. It must also update the current route integration verifier/test contract as an amendment from latest GOLDEN rather than retaining two competing runtime consumers.

Concrete Bubble pixel radii/layout policy remains intentionally unresolved because current user/Kairos authority still does not supply min/max pixel radius, responsive breakpoints, collision packing, or label-fit values; those must not be guessed.
