# Kairos Gate 365 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #365 / run `34541322664` / job `103084382367` completed full SUCCESS on 2026-09-10 at exact head `0e6f31c4f6bfb262d18fe7b03aeb8c357676028e`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_REACT_RADIUS_SCALE_VIEW_MODEL_FOUNDATION_CANDIDATE_2026-09-11.zip`

Canonical candidate identity: size `1,698,058` bytes; SHA-256 `09123bb0172535aac5191a5c642541e2b7e6976020a0bb3609ee30e0f546439c`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10177672886`, wrapper size `1,450,017` bytes, digest `sha256:ef5e3fb110ea23c784a30bcd8bd5b9bd271d3af155466a9417e4dc8c11b81512`.
- `KAIROS_GATE_EVIDENCE`: id `10177673440`, wrapper size `1,297` bytes, digest `sha256:e57c0660062e8d99e0e7c9eddc0586a67a3a6fce2ae734613fe89620d3e39e18`.

Every required canonical stage passed: exact uploaded archive identity/integrity, authoritative Gate364 base/candidate extraction, exact controlled five-file React radius-scale view-model scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated current/lower-owner verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload and gate-evidence upload.

## Canonically released responsibility

Gate365 releases `src/app/homeDashboardLiveCryptoBubbleReactRadiusScaleViewModel.ts` as a pure app-level composition seam between the released React area-weight view model and the released Gate364 normalized radius-scale projection.

`projectHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)` owns only this deterministic composition:
1. accept one exact `HomeDashboardLiveCryptoBubbleReactAreaWeightViewModel`;
2. preserve that exact `areaWeightViewModel` reference;
3. preserve its exact `runtimeState` reference;
4. when `areaWeightProjection` is `null`, expose `radiusScaleProjection: null`;
5. otherwise pass that exact `areaWeightProjection` reference once to released `projectHomeDashboardLiveCryptoBubbleRadiusScales(...)`; and
6. return the resulting radius-scale projection without filtering, sorting, reordering or reinterpreting entries.

Gate364 remains the normalized `sqrt(areaWeight)` arithmetic owner. Gate365 adds no independent market, runtime, React lifecycle or pixel-geometry truth.

## Explicit non-scope

Gate365 does **not** own or change:
- React hooks, retained state, effects, memoization, clocks or timers;
- provider/request/response/acquisition/retry behavior;
- universe eligibility, ranking, Top-30, stablecoin exclusion, tie-break, movement or freshness product policy;
- pixel radius, minimum/maximum visible radius, viewport/device breakpoints;
- collision/packing/layout, coordinates, labels, interaction or hit targets;
- palette/theme-token selection, CSS, stale/expired opacity or animation;
- `HomeRoute` wiring;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward or Calculation Brain.

Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

## Next dependency-safe responsibility proof

Fresh Gate365 scope proves the new React radius-scale view-model projector was added without changing the already released React runtime hook path. The existing `useHomeDashboardLiveCryptoBubbleReactAreaWeightViewModel(...)` still owns the thin React delegation from caller-owned runtime inputs into the Gate351 runtime binding and Gate354 area-weight view model, while Gate365 now provides the pure downstream projector from that exact area-weight view model to radius-scale evidence.

The smallest dependency-safe next responsibility is therefore a **thin React radius-scale hook composition seam** only:
1. accept caller-owned `readObservedAt`, `readEvaluationTimeMs` and the exact existing `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` without inventing defaults;
2. call released `useHomeDashboardLiveCryptoBubbleReactAreaWeightViewModel(...)` exactly once per render with those exact references;
3. pass the exact returned `HomeDashboardLiveCryptoBubbleReactAreaWeightViewModel` reference once to released `projectHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)`;
4. return the exact Gate365 `HomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel` result unchanged; and
5. add no second state/effect owner, clocks, provider/product logic, pixel sizing, viewport/collision/layout, labels, palette/CSS/motion, `HomeRoute`, persistence or Your Trades behavior.

This connects the newly released normalized radius evidence to the existing React lifecycle path while still deferring responsive pixel geometry and visible Bubble rendering to later controlled presentation owners.