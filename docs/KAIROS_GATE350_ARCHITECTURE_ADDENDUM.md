# Kairos Gate 350 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #350 / run `34454131565` / job `102796440504` completed full SUCCESS on 2026-09-10 at exact head `19f6660d0daaf0f028a1a665f4fe6274092ed60f`.

Exact canonical candidate:
`KAIROS_BINANCE_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_PRESENTATION_OBSERVED_RUNTIME_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-10.zip`

Canonical candidate identity: size `1,623,913` bytes; SHA-256 `0df691a0f561841b518a74c97850da42e233d7a24e9669cc70dbe282bc2db619`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10143072131`, wrapper size `1,389,278` bytes, digest `sha256:c49c83250d3e9a082b97b6f8c8daf41e9b1a82cca93ac28b570649d9d2f62e1e`.
- `KAIROS_GATE_EVIDENCE`: id `10143072808`, wrapper size `1,356` bytes, digest `sha256:c6b2702cf8f323c7a5322bd41b35f3189a38ef54dbd91813727599f10de17068`.

The exact-run current-candidate artifact was independently downloaded after the run. Its wrapper contained exactly one nested candidate with the exact filename, size, SHA-256 and root above. Every required canonical stage passed: exact archive identity/integrity, Gate349 base/candidate extraction, exact five-file scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript/build, dedicated runtime verifier, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both canonical uploads.

## Canonically released responsibility

Gate350 releases `src/app/binanceHomeDashboardLiveCryptoBubblePresentationObservedRuntimeComposition.ts` as the app-level Binance Home Dashboard Live Crypto Bubble presentation-observed runtime composition below React.

`startBinanceHomeDashboardLiveCryptoBubblePresentationObservedRuntime(...)` owns only this composition:

1. preserve exact caller-owned `readObservedAt` and `readEvaluationTimeMs` sources;
2. preserve Gate345 universe and browser-lifecycle options;
3. require one caller-owned Gate346 presentation policy;
4. accept one optional Gate349 semantic presentation-state observation sink;
5. construct exactly one Gate349 presentation-state observation adapter;
6. pass that exact adapter unchanged as Gate345's `observationSink`;
7. delegate exactly once to `startBinanceHomeDashboardLiveCryptoBubbleObservedRuntime(...)`; and
8. return Gate345's bootstrap result unchanged.

Provider/acquisition truth, universe policy, raw Bubble metric truth, semantic presentation-state truth, runtime lifecycle, and later React state/rendering remain separate owners.

## Explicit non-scope

Gate350 does **not** own or change:
- a second raw metric/freshness sink or an internal clock;
- Binance transport, response mapping, request execution or provider semantics;
- active-USDT eligibility, stablecoin exclusion configuration, quote-volume ranking, symbol tie-break or Top-N policy;
- freshness thresholds, acquisition cadence, browser visibility semantics or lower runtime/session mutation;
- design-system palette/tokens, bubble radius/geometry/collision/layout, labels/accessibility copy, interactions or animation;
- DOM/React component lifecycle/state or visible Home/Bubble rendering;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, Calculation Brain, navigation or transitions.

## Next dependency-safe responsibility proof

Fresh Gate350 GOLDEN source now closes the app-level semantic-runtime composition seam that Gate349 explicitly required before React consumption.

Evidence:
- Gate350 exposes one callback-shaped `HomeDashboardLiveCryptoBubblePresentationStateObservationSink` through the exact Binance Home observed runtime without making React own provider/acquisition/business truth.
- `src/app/HomeRoute.tsx` remains the P21.1 Home presentation owner and still renders only the dashboard foundation/empty state.
- Repository search finds no React production consumer of `startBinanceHomeDashboardLiveCryptoBubblePresentationObservedRuntime(...)`.
- P21.1 established `HomeRoute` as the presentation owner but deliberately left live subscription/provider rendering outside that initial foundation.
- The caller-owned stablecoin exclusion set and Gate346 neutral-movement threshold remain explicit product/configuration inputs; this boundary must not guess or hard-code them merely to make the route live.

Therefore the smallest dependency-safe next responsibility is a **Home Live Crypto Bubble React lifecycle/state binding foundation**, not visible bubble geometry yet.

Freeze the next contract before candidate construction:

1. add one React hook/binding below `HomeRoute` that accepts the already-owned `readObservedAt`, `readEvaluationTimeMs`, universe options, browser-lifecycle options, and Gate346 presentation policy as explicit caller-owned inputs;
2. start exactly one Gate350 presentation-observed runtime from an effect and close that exact runtime on teardown, including safe cleanup when asynchronous bootstrap resolves after unmount;
3. keep the latest Gate349 semantic observation in React-local presentation state without rewriting the metric observation or projection result;
4. expose explicit bootstrap/observation/error state so missing, stale, expired and deterministic projection failures remain distinguishable rather than being coerced to usable market facts;
5. preserve the last semantic observation when an acquisition error is reported, matching the P21 rule that acquisition failure does not erase the last authoritative snapshot;
6. do not choose concrete stablecoin exclusions, neutral threshold values, Bubble radius scaling, palette tokens, geometry/collision/layout, labels/copy, animation or interactions in this slice; and
7. do not yet make `HomeRoute` visibly render Bubble circles. Visible Bubble rendering should consume this released React state boundary in a later controlled presentation slice.

This keeps the next change dependency-safe and avoids inventing product policy values that current source deliberately leaves caller-owned.
