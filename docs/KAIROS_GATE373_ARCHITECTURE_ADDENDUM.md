# Kairos Gate373 Architecture Addendum — HomeRoute Radius-Scale Text Runtime Integration

Canonical authority: `Kairos Controlled Roadmap Gate` #373 / run `34558890062`, job `verify-current-candidate` / `103137319598`, exact head `5de116c554be62079fa5132385748b581584e12c`, FULL SUCCESS.

Exact promoted candidate: `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_HOME_ROUTE_RADIUS_SCALE_TEXT_RUNTIME_INTEGRATION_AMENDMENT_CANDIDATE_2026-09-11.zip`, size `1,726,756` bytes, SHA-256 `2097fc3a416362e7cbeca85eb8b0dc1fb6878c57d6c207c560f43e182e9d05e3`, Git blob `17235b1724f7fc55579b637b44f30d28d25fdc0d`, root exactly `kairos_p76/`.

## Canonical ownership after Gate373

- `HomeRoute` remains the existing Home route/presentation integration owner.
- `createHomeDashboardLiveCryptoBubbleDefaultRuntimeProductConfiguration()` remains the released default Live Crypto Bubble product-configuration owner.
- Gate373 replaces the superseded configured area-weight textual runtime consumer with exactly one released Gate372 `HomeDashboardLiveCryptoBubbleConfiguredBrowserRadiusScaleTextEvidenceRuntime` consumer.
- Gate372 remains the configured-browser normalized-radius textual runtime composition owner below the route.
- Gate371 remains the normalized-radius textual evidence presenter owner.
- Gate369 remains the configured browser radius-scale hook owner.
- Gate364 and lower released owners retain normalized radius/area/presentation/market truth and arithmetic.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

No duplicate Live Crypto runtime consumer is released by Gate373.

## Explicit boundary retained

Gate373 does **not** select or own:

- minimum/maximum Bubble pixel radii;
- responsive radius breakpoints;
- viewport measurement or geometry;
- collision/packing/layout behavior;
- label-fit or hit-target policy;
- SVG/canvas renderer choice;
- Bubble palette/CSS/theme/motion;
- provider/acquisition/freshness/ranking/Top-N/stablecoin semantics;
- persistence/Saved Analysis;
- Your Trades/journal/chart/Risk-Reward/Calculation Brain truth.

The released radius-scale projection intentionally stops at normalized `radiusScale`. Current generic design-system geometry tokens do not establish Live Crypto Bubble pixel-radius, breakpoint, collision or label-fit values. Therefore concrete visual Bubble sizing/layout remains a separate future product/design-policy boundary and must not be guessed from this canonical PASS.

## Next dependency boundary

Fresh Gate373 source leaves one source-proven downstream renderer seam: convert the already-released normalized radius evidence into visible Bubble geometry without reinterpreting market truth. The smallest safe technical step may be a **caller-owned pixel-radius policy contract** only, with no default concrete values and no renderer/layout behavior, if implemented as a later controlled candidate. Actual default min/max radii, responsive breakpoints, collision/packing, label-fit and visible rendering policy still require explicit authoritative product/design values.
