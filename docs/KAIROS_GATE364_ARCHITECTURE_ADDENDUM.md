# Kairos Gate 364 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #364 / run `34538038056` / job `103074092094` completed full SUCCESS on 2026-09-10 at exact head `1ae6a902d412f767ccd69dfdc1c390e087d91e8e`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_RADIUS_SCALE_PROJECTION_FOUNDATION_CANDIDATE_2026-09-11.zip`

Canonical candidate identity: size `1,692,716` bytes; SHA-256 `0c728ba16c6559e62523d1d0cbb1057d59ab86dc4f7febefdeefd4edece4ebf1`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10176507565`, wrapper size `1,445,463` bytes, digest `sha256:b6d8d95362e25302111b478ba63fd081c0626b06325c98f6fad1c06a7fd1b36f`.
- `KAIROS_GATE_EVIDENCE`: id `10176507860`, wrapper size `1,851` bytes, digest `sha256:6e987909f3e91b504ed743b9502dfbcbbcb92ad953d264a4355230fb60ad4096`.

Every required canonical stage passed: exact uploaded archive identity/integrity, authoritative Gate363 base/candidate extraction, exact controlled five-file radius-scale projection scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated radius-scale/lower-owner verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload and gate-evidence upload.

Independent exact-run artifact inspection also re-proved one nested candidate with the exact filename, size and SHA-256 above, clean ZIP integrity, root exactly `kairos_p76/`, no absolute/traversal paths and no symlink entries.

## Canonically released responsibility

Gate364 releases `src/application/dashboard/homeDashboardLiveCryptoBubbleRadiusScaleProjection.ts` as the presentation-only normalized radius-scale owner immediately downstream of Gate352 area-weight evidence.

It owns only this deterministic projection:
1. accept one exact `HomeDashboardLiveCryptoBubbleAreaWeightProjectionResult`;
2. preserve upstream failure evidence as `area-weight-projection-invalid`;
3. preserve successful entry order and each exact `areaWeightEntry` reference;
4. map `areaWeight: null` to `radiusScale: null`;
5. validate each present numeric area weight is finite and within `[0,1]`;
6. map each valid present area weight using the already approved presentation transform `radiusScale = sqrt(areaWeight)`; and
7. fail closed with the offending `entryIndex` if a successful upstream result contains non-finite or out-of-range normalized area evidence.

The exact boundaries `0 -> 0` and `1 -> 1` remain preserved.

## Explicit non-scope

Gate364 does **not** own or change:
- pixel radius, minimum/maximum visible radius or viewport/device breakpoints;
- collision/packing/layout, coordinates, label fit, interaction or hit targets;
- palette/theme-token selection, CSS, stale/expired opacity or animation;
- provider/request/response/acquisition/retry behavior;
- universe eligibility, ranking, Top-30, stablecoin exclusion, symbol tie-break or neutral-movement policy;
- freshness/cadence/lifecycle/timer/clock semantics;
- HomeRoute wiring, persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward or Calculation Brain.

Live Crypto Bubble and Your Trades Bubble remain separate authoritative truth domains.

## Next dependency-safe responsibility proof

Fresh Gate364 source proves the new normalized radius-scale projection is intentionally not yet consumed by the existing React-ready Bubble view-model path. The current app-level composition `projectHomeDashboardLiveCryptoBubbleReactAreaWeightViewModel(...)` stops at Gate352 `areaWeightProjection`, while `projectHomeDashboardLiveCryptoBubbleRadiusScales(...)` is a pure downstream owner with no React, DOM or pixel policy.

Moving immediately to pixel circles would couple a newly released deterministic projection to still-separate responsive geometry/layout choices. The smallest dependency-safe next responsibility is therefore an **app-level React radius-scale view-model composition seam** only:
1. accept the exact existing `HomeDashboardLiveCryptoBubbleReactAreaWeightViewModel` produced by the released app/runtime path;
2. preserve the exact existing `runtimeState` and existing area-weight view-model reference;
3. when `areaWeightProjection` is `null`, expose `radiusScaleProjection: null`;
4. otherwise delegate the exact `areaWeightProjection` reference once to Gate364 `projectHomeDashboardLiveCryptoBubbleRadiusScales(...)`;
5. return the resulting radius-scale projection without reinterpreting or filtering entries; and
6. add no state/effects, clocks, network/provider policy, product defaults, pixels, min/max radius, viewport, collision/layout, labels, palette/CSS, motion, persistence or Your Trades behavior.

This keeps the deterministic normalized geometry chain connected without prematurely choosing renderer/pixel policy. A later controlled presentation slice can consume that app-level radius-scale view model to select responsive pixel bounds/layout while retaining one owner per responsibility.