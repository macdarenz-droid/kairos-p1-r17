# Kairos Gate 368 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #368 / run `34547310880` / job `103102623912` completed full SUCCESS on 2026-09-11 at exact head `a265b1d73092a3a695b2b4f2bfeda25b63a280ee`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_BROWSER_RADIUS_SCALE_HOOK_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-11.zip`

Canonical candidate identity: size `1,708,601` bytes; SHA-256 `737394acc0e44408e6305320a35a3da5050656b227f11c7d2ceed67849bc2e06`; Git blob `72087c5b83fb6cc800dcfec0a00f6904b2155362`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10179763678`, wrapper size `1,457,991` bytes, digest `sha256:600d707faba3b1e9be801380fd9a6ab7e5e272604a55f7ca7f85ea96a3b6edb6`.
- `KAIROS_GATE_EVIDENCE`: id `10179763937`, wrapper size `1,323` bytes, digest `sha256:287b2d394286ec7a82be3a0d2c3bcef820b38d35e434eb3dbc314d90612d4b36`.

Independent exact-run artifact inspection re-proved that `KAIROS_CURRENT_CANDIDATE` contains exactly the canonical candidate above with the exact size/SHA-256, clean ZIP integrity, root exactly `kairos_p76/`, no absolute/traversal paths and no symlinks. The gate-evidence artifact contains exactly the browser radius-scale hook-composition foundation report.

Every required canonical stage passed: exact uploaded archive identity/integrity, authoritative Gate366 base/candidate extraction, exact controlled five-file browser radius-scale hook-composition scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated current/lower-owner verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload and gate-evidence upload.

Gate367 / run `34546978176` remains failed evidence only. Its failure was a gate-configuration archive-identity defect before candidate extraction. The corrected Gate368 constants bound the same independently verified helper-tested candidate without widening candidate scope. Gate367 never became a base or GOLDEN.

## Canonically released responsibility

Gate368 releases `src/app/useHomeDashboardLiveCryptoBubbleBrowserRadiusScaleViewModel.ts` as the browser-specific injection edge immediately above the Gate366 React radius-scale hook.

`useHomeDashboardLiveCryptoBubbleBrowserRadiusScaleViewModel(...)` owns only this delegation:
1. accept one exact caller-owned `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` reference;
2. call released Gate366 `useHomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel(...)` exactly once;
3. inject Gate358 `readHomeDashboardLiveCryptoBubbleBrowserObservedAt` unchanged as the acquisition-observation source;
4. inject Gate358 `readHomeDashboardLiveCryptoBubbleBrowserEvaluationTimeMs` unchanged as the freshness-evaluation source;
5. forward the exact caller-owned runtime-options reference unchanged; and
6. return the exact Gate366 `HomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel` result unchanged.

Gate351 remains the React lifecycle/state owner. Gate352 remains normalized area-weight arithmetic owner. Gate354 remains the area-weight view-model owner. Gate355 remains area-weight React-hook composition owner. Gate358 remains the browser wall-clock owner. Gate360 remains runtime product-configuration-to-binding-options composition owner. Gate364 remains normalized `sqrt(areaWeight)` radius-scale arithmetic owner. Gate365 remains pure radius-scale view-model projection owner. Gate366 remains the injected React radius-scale hook-composition owner. Gate368 adds only the browser clock-injection edge.

## Explicit non-scope

Gate368 does **not** own or change:
- React state/effects/memo/cache or any second runtime state owner;
- `Date.now()`, `new Date()`, timer/cadence/page-visibility implementation, or freshness thresholds;
- provider/request/response/acquisition/retry behavior;
- universe eligibility, ranking, Top-30, stablecoin exclusion, tie-break, movement or freshness product policy;
- normalized radius arithmetic beyond consuming Gate364 evidence;
- pixel radius, minimum/maximum visible radius, viewport/device breakpoints, collision/packing/layout, coordinates, labels, interactions or hit targets;
- palette/theme-token selection, CSS, stale/expired opacity, motion or animation;
- `HomeRoute` wiring or route ownership;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward or Calculation Brain.

Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

## Next dependency-safe responsibility proof

A deeper fresh source trace after the canonical PASS shows that the released browser radius-scale hook still accepts `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions`, while the actual `HomeRoute` caller owns a `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration`. Gate360 already owns the pure conversion from that exact configuration to runtime-binding options, and Gate361 established the same configuration-to-browser-runtime composition pattern for the existing textual-evidence path. No released radius-scale-specific configured hook closes this exact dependency edge yet.

Therefore the smallest dependency-safe next responsibility is **configured browser React radius-scale hook composition only**:
1. accept one exact caller-owned `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration` reference;
2. delegate that exact configuration once to released Gate360 `composeHomeDashboardLiveCryptoBubbleRuntimeBindingOptions(...)`;
3. pass the exact returned runtime-options object once to released Gate368 `useHomeDashboardLiveCryptoBubbleBrowserRadiusScaleViewModel(...)`;
4. return the exact Gate368 radius-scale view-model result unchanged.

This seam must add no state/effects/memo/cache, browser clocks, provider/product policy, radius arithmetic, pixel min/max values, viewport/collision/layout/labels, palette/theme/CSS/motion, route mutation, persistence or Your Trades behavior. It only closes the already-released product-configuration-to-browser-radius-scale-hook dependency edge.

Responsive pixel sizing remains a later presentation responsibility. The user-approved product contract does not specify concrete minimum/maximum pixel radii, breakpoints, collision packing or label-fit values, so those presentation-policy choices must not be invented by this technical composition slice.