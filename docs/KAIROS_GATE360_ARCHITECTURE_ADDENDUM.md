# Kairos Gate 360 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #360 / run `34508122572` / job `102975215773` completed full SUCCESS on 2026-09-10 at exact head `c2df9e9d801196cb74168a6e08c267ee926aca5b`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_RUNTIME_OPTIONS_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-10.zip`

Canonical candidate identity: size `1,671,496` bytes; SHA-256 `cce4ef61ced3996d5a1b6c5efd3c241ef218acca33dfd10ed8721b7af9966273`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10164999670`, wrapper size `1,428,677` bytes, digest `sha256:19a8708df29cd0828552b8da6d30b8267b5edb0b957060d7d42a31e941bb7b2a`.
- `KAIROS_GATE_EVIDENCE`: id `10165000388`, wrapper size `1,404` bytes, digest `sha256:9db155897d9889c34d17fe1952820bdd64afa7eec880eca033a2a0b71169779a`.

Every required canonical stage passed: exact archive identity/integrity, Gate359 base/candidate extraction, exact five-file runtime-options composition scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated runtime-options composition verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both canonical uploads.

Independent exact-run artifact inspection re-proved the nested candidate filename, exact size and SHA-256, root exactly `kairos_p76/`, clean ZIP integrity, and no absolute paths, traversal paths, or symlinks.

## Canonically released responsibility

Gate360 releases `src/app/homeDashboardLiveCryptoBubbleRuntimeOptionsComposition.ts` as the pure application-level composition boundary for the two still-caller-owned Home Live Crypto Bubble product configuration values required by the already-released runtime binding.

It owns only this composition:
1. accept the exact caller-owned `excludedStablecoinBaseAssets: ReadonlySet<string>` reference;
2. accept the exact caller-owned `neutralMaxAbsoluteMovementPercent: DecimalString` value;
3. construct the existing `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions` shape;
4. delegate the exact set reference to `universe.excludedStablecoinBaseAssets`;
5. delegate the exact Decimal value to `presentationPolicy.neutralMaxAbsoluteMovementPercent`;
6. deliberately omit `topNCount`, leaving the already-released Top-30 default as its sole owner; and
7. deliberately omit browser lifecycle overrides, leaving the already-released lifecycle and visible-cadence defaults as their sole owners.

Gate359 retains the browser clock-to-textual-runtime composition edge. Gate358 retains concrete browser wall-clock ownership. Earlier released owners retain universe eligibility semantics, Top-N policy, lifecycle/cadence, freshness policy, presentation-state mapping, textual evidence, provider/data acquisition, and lower-layer truth.

## Explicit non-scope

Gate360 does **not** own or change:
- the concrete excluded-stablecoin set contents;
- the concrete neutral-movement threshold;
- Top-N default ownership or ranking/tie-break semantics;
- browser lifecycle defaults, visible acquisition cadence, page visibility, freshness thresholds, clocks, timers, provider transport, acquisition, or retry;
- React state/effects/memo/callback ownership;
- `HomeRoute` or router wiring;
- Bubble radius/pixel scaling, collision/layout, label placement, SVG/canvas, interaction, CSS, palette, motion, or transitions;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, or Calculation Brain.

The authoritative Bubble-size metric remains 24h quote volume. Gate360 adds no geometry and chooses no new product value.

## Next dependency-safe responsibility proof

Fresh Gate360 GOLDEN now provides a pure producer of `HomeDashboardLiveCryptoBubbleReactRuntimeBindingOptions`, while Gate359 already provides the browser textual-evidence runtime component that consumes that exact options type. No released owner currently composes those two adjacent application boundaries together.

Production `HomeRoute` remains a placeholder and should not duplicate the Gate360 options shape, Gate359 browser-clock injection, or lower runtime ownership. The concrete stablecoin exclusion contents and neutral-movement threshold also remain unreleased material product choices and must not be guessed by route wiring.

The smallest safe **technical** responsibility that can proceed without choosing either product value is therefore one thin **configured browser textual-runtime composition edge** below `HomeRoute`:
1. accept exactly one `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration` value from its caller;
2. call `composeHomeDashboardLiveCryptoBubbleRuntimeBindingOptions` exactly once with that exact configuration;
3. render `HomeDashboardLiveCryptoBubbleBrowserTextEvidenceRuntime` exactly once with the composed options;
4. introduce no product defaults, validation, cloning, arithmetic, clocks, state, effects, timers, provider behavior, geometry, styling, route wiring, persistence, or Your Trades ownership; and
5. leave the eventual application/route owner responsible only for supplying the two still-explicit product configuration values after those values are authoritatively released.

This closes the remaining purely technical Gate360-to-Gate359 composition seam without prematurely assigning the still-material user-facing product configuration.