# Kairos Gate 361 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #361 / run `34512298327` / job `102989143225` completed full SUCCESS on 2026-09-10 at exact head `cffc52d9d78de9864c343496f3d1059bf35914c9`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_CONFIGURED_TEXT_EVIDENCE_RUNTIME_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-10.zip`

Canonical candidate identity: size `1,676,282` bytes; SHA-256 `89f14c02b5387382c5de67793212ff9de849676e0fe21deda72d329df46ee0fc`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10166724053`, wrapper size `1,432,517` bytes, digest `sha256:d613f1653bcfbe38558c368340b33cf21be81f6472fe537586bb55ffc1f1752c`.
- `KAIROS_GATE_EVIDENCE`: id `10166724500`, wrapper size `1,409` bytes, digest `sha256:94e9e601ac7b9d6e58573e03e6144fb0355315c567916df0cde9b7e1e878b22e`.

Every required canonical stage passed: exact archive identity/integrity, Gate360 base/candidate extraction, exact five-file configured text-evidence runtime-composition scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated verifier/runtime, focused regression, full unit regression, full controlled-roadmap regression, historical closures, and both canonical uploads.

Independent exact-run artifact inspection re-proved the nested candidate filename, exact size and SHA-256, root exactly `kairos_p76/`, clean ZIP integrity, and no absolute paths, traversal paths, or symlinks.

## Canonically released responsibility

Gate361 releases `src/app/HomeDashboardLiveCryptoBubbleConfiguredTextEvidenceRuntime.tsx` as the thin configured browser textual-runtime composition edge immediately below eventual `HomeRoute` integration.

It owns only this composition:
1. accept one exact caller-owned `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration` value;
2. delegate that exact configuration once to Gate360 `composeHomeDashboardLiveCryptoBubbleRuntimeBindingOptions(...)`;
3. receive the released runtime-options object without reinterpretation;
4. render Gate359 `HomeDashboardLiveCryptoBubbleBrowserTextEvidenceRuntime` exactly once with that exact options object; and
5. add no hidden defaults, policy, clocks, state, provider behavior, geometry, styling, route behavior, persistence, or Your Trades logic.

Gate360 remains runtime-options composition owner. Gate359 remains browser textual-evidence runtime owner. Gate358 remains concrete browser wall-clock owner. Earlier released owners retain provider/acquisition, universe/ranking, freshness, React runtime state, semantic presentation, and normalized area-weight truth.

## Explicit non-scope

Gate361 does **not** own or change:
- the concrete excluded-stablecoin set contents;
- the concrete neutral-movement threshold;
- Top-N default/ranking/tie-break semantics;
- browser lifecycle, visible 5-second acquisition cadence, freshness thresholds, clocks, timers, provider transport, acquisition, or retry;
- `HomeRoute` or router wiring;
- Bubble radius/pixel scaling, square-root conversion, minimum/maximum radius, viewport/device geometry, collision/layout, label placement, SVG/canvas, interaction, CSS, palette, dimming strength, motion, or transitions;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward, or Calculation Brain.

The authoritative Bubble-size metric remains 24h quote volume. The released Gate352 `areaWeight` remains only normalized visual-area evidence and does not itself define pixel radius or layout.

## Next dependency-safe responsibility proof

Fresh Gate361 GOLDEN closes the remaining purely technical composition seam from caller-owned runtime product configuration through the browser textual-evidence runtime. `src/app/HomeRoute.tsx` remains a placeholder, and current released owners still intentionally leave the following user-facing choices external:
- the concrete stablecoin base-asset exclusion set;
- the concrete `neutralMaxAbsoluteMovementPercent` value; and
- visible Bubble geometry/design policy such as radius scaling, minimum/maximum visible radius, collision/layout, labels, palette and dimming strength.

Earlier canonical architecture explicitly left pixel-radius/square-root/min-max geometry and visible Bubble rendering to a later controlled presentation owner rather than inventing those semantics. Gate361 introduces no new authority that resolves those choices.

Therefore the next smallest dependency-safe responsibility is **not another hidden technical composition shim**. The next responsibility is an explicit product/design-policy release for the still-unowned visible Live Crypto Bubble presentation inputs before `HomeRoute` or a visible Bubble renderer can be wired without guessing. Until such values/policy are authoritatively selected, implementation must preserve Gate361 as the latest good technical boundary and must not invent defaults.

This is a deliberate no-guess boundary, not an engineering failure. It prevents route/UI code from becoming a second owner of product semantics that the current canonical chain intentionally keeps caller-owned.