# Kairos Gate380 Architecture Addendum — Configured Browser Pixel-Radius Hook Composition

Canonical authority: `Kairos Controlled Roadmap Gate` #380 / run `34579897908`, job `verify-current-candidate` / `103200776280`, exact head `2c62b72c52c0a43131ca0519b48c4e32fe80e34b`, FULL SUCCESS.

Exact promoted candidate: `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_CONFIGURED_BROWSER_PIXEL_RADIUS_HOOK_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-11.zip`, size `1,766,211` bytes, SHA-256 `a4530878e81b8683f90a523652066170c284a6ea7d9fd4610e1763cf1968785e`, root exactly `kairos_p76/`.

Exact-run canonical artifacts:
- `KAIROS_CURRENT_CANDIDATE` artifact `10191514053`, wrapper size `1,505,471` bytes, digest `sha256:4900837e6488e4c58790012a56bd1272373f5d0b4fc12a3f3cf8ba110222a65c`.
- `KAIROS_GATE_EVIDENCE` artifact `10191514483`, wrapper size `1,531` bytes, digest `sha256:7af9266160675682ac54084a41bde5e3345c3277e36b51a894387ece39d2a09b`.

Every canonical stage succeeded, including exact uploaded archive identity/integrity, exact five-file Gate379→Gate380 scope with zero removals, deterministic install, exact Lightweight Charts 5.2.1, production TypeScript/build, dedicated configured-browser pixel-radius hook-composition verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload, and gate-evidence upload. Independent extraction of the exact-run `KAIROS_CURRENT_CANDIDATE` artifact re-proved the sole nested candidate's exact filename, size and SHA-256 above.

## Canonically released responsibility

Gate380 releases `src/app/useHomeDashboardLiveCryptoBubbleConfiguredBrowserPixelRadiusViewModel.ts` as the configured product/browser composition edge above the released Gate379 browser pixel-radius hook.

`useHomeDashboardLiveCryptoBubbleConfiguredBrowserPixelRadiusViewModel(...)`:
- accepts the exact caller-owned `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration` and exact caller-supplied `HomeDashboardLiveCryptoBubblePixelRadiusPolicy`;
- calls `composeHomeDashboardLiveCryptoBubbleRuntimeBindingOptions(configuration)` exactly once;
- passes the exact composed runtime-binding options and exact caller policy exactly once to `useHomeDashboardLiveCryptoBubbleBrowserPixelRadiusViewModel(...)`;
- returns the exact Gate379 result unchanged.

## Ownership retained below and above Gate380

- Gate379 remains the browser pixel-radius hook-composition owner that injects the released Gate358 browser wall-clock sources into the released generic React pixel-radius hook.
- Gate378 remains the generic React pixel-radius hook-composition owner.
- Gate377 remains the pure normalized-radius-view-model-to-pixel-radius-view-model projector owner.
- Gate376 remains the pure pixel-radius textual-evidence presenter owner.
- Gate375 remains the provider-neutral CSS-pixel projection owner.
- Gate374 remains the caller-owned pixel-radius policy contract/validator.
- Gate351 and its released configured runtime composition remain the runtime-binding product-configuration owner below this thin hook edge.
- Lower released owners retain the single React runtime lifecycle/state chain, provider acquisition, freshness, universe, ranking, movement and observation responsibilities.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

Gate380 adds no second state/effect/cache/timer owner, no clock implementation, no viewport measurement, no default CSS-pixel sizing, no radius-policy arithmetic or validation, no collision/packing/layout/renderer/style/route ownership, no persistence and no Your Trades truth.

## Explicit non-scope retained

Gate380 does **not** own or choose:
- concrete/default minimum or maximum Bubble radii;
- responsive breakpoints or browser viewport measurement;
- collision detection, packing, layout, label-fit or hit-target policy;
- SVG/canvas/DOM renderer choice;
- Bubble palette, CSS, theme, dimming strength or motion;
- `HomeRoute` wiring or a second Live Crypto runtime consumer;
- Binance/provider transport, acquisition, retries, cadence, freshness, universe, ranking, Top-N, stablecoin exclusion, movement or market-truth semantics;
- persistence, Saved Analysis, Your Trades, journal, chart, Risk/Reward or Calculation Brain truth.

No concrete visual sizing value is established by this canonical PASS. Those values remain caller/product/design-owned under the approved P21 policy and must not be guessed.

## Next dependency boundary

Fresh Gate380 source now provides the configured browser pixel-radius hook while the released Gate376 pixel-radius textual-evidence presenter already owns truthful CSS-pixel evidence presentation. The released configured-browser radius-scale text-evidence runtime establishes the matching composition pattern: one configured browser hook call followed by one pure presenter delegation, without introducing a second runtime owner.

The smallest dependency-safe continuation is therefore a **configured browser pixel-radius text-evidence runtime composition boundary**: accept the exact caller product configuration plus exact caller pixel-radius policy; call `useHomeDashboardLiveCryptoBubbleConfiguredBrowserPixelRadiusViewModel(...)` exactly once; when the released model contains an authoritative pixel-radius projection, pass that exact projection reference once to `HomeDashboardLiveCryptoBubblePixelRadiusTextEvidence`; when the model has no projection, render only an explicit unavailable/no-observation state without inventing radius data.

That runtime seam must own no pixel-radius defaults, no viewport measurement, no collision/packing/layout/renderer styling, no provider/market semantics, no persistence, no Your Trades truth and no navigation/transition ownership. Visible Bubble rendering remains a later controlled presentation responsibility.
