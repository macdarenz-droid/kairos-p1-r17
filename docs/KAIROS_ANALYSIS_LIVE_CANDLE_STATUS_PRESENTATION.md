# Kairos Analysis live-candle status presentation

Date: 13 September 2026  
Roadmap owner: item 4 live candle continuity presentation  
UI visible: no

## Responsibility

This bounded item-4 amendment projects the exact raw availability, activation, connection, disposition, recovery and error evidence released through Gate432 into immutable presentation facts for a later Analysis workspace mount. A live label is possible only from the exact `live` connection state after successful authoritative-history activation. Loading, connecting, bounded reconnect, gap recovery, hidden/offline pause, empty history, provider unavailability, scope rejection, renderer failure and safe generic error states remain distinct.

The projection never exposes raw error values in copy. It preserves the released fail-closed behavior and does not convert missing or failed evidence into a live claim.

## Exclusions

This slice does not mount the Analysis route/workspace, create a canvas or render the status. It starts no history request, stream, timer, subscription or selection. It adds no provider default/expansion, persistence, IndexedDB access, journal mutation, calculation, execution inference, FX, drawing, marker or Live Bubble behavior. UI visible: no. P21 remains active.

## Evidence

- The dedicated verifier requires exact live, browser availability, history failure, scope failure, renderer failure and gap-recovery branches while rejecting React, DOM, provider execution, persistence, journal and calculation ownership.
- Focused tests prove every raw lifecycle class, provider-safe copy, immutable output and the rule that only an exact live connection can claim live candles.
- TypeScript, production build, all current/historical/browser regressions and exact candidate/artifact verification remain mandatory before promotion.
