# Kairos Gate376 Architecture Addendum — Live Crypto Bubble Pixel-Radius Text Evidence Presentation

Canonical authority: `Kairos Controlled Roadmap Gate` #376 / run `34567414952`, job `verify-current-candidate` / `103162269063`, exact head `9ec252f5cc62516fe0405225e932db5a87d8d4e5`, FULL SUCCESS.

Exact promoted candidate: `KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_PIXEL_RADIUS_TEXT_EVIDENCE_PRESENTATION_FOUNDATION_CANDIDATE_2026-09-11.zip`, size `1,743,965` bytes, SHA-256 `dbadf07106845688f079474dc05edcb16da0e732224447169d35b8e6d89ecd00`, Git blob `8b14ba725aaa18addd29c71ea39fcf1fa61c1609`, root exactly `kairos_p76/`.

Exact-run canonical artifacts:
- `KAIROS_CURRENT_CANDIDATE` artifact `10186745585`, wrapper size `1,487,398` bytes, digest `sha256:21f44ee54247b94bc19df6940b79019754b2d3dec6ee5d653c2fba7fdb086b76`.
- `KAIROS_GATE_EVIDENCE` artifact `10186745910`, wrapper size `1,235` bytes, digest `sha256:a32a1f889c30f2ec38686ffc10fd7ae4b7fa30f74830138b91a7614edc0233a2`.

Every canonical stage succeeded, including exact archive identity/integrity, exact five-file Gate375→Gate376 scope with zero removals, deterministic install, exact Lightweight Charts 5.2.1, production TypeScript/build, dedicated pixel-radius text-evidence presentation verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload, and gate-evidence upload.

## Canonically released responsibility

Gate376 releases `src/app/HomeDashboardLiveCryptoBubblePixelRadiusTextEvidence.tsx` as a pure React evidence presenter over the exact Gate375 `HomeDashboardLiveCryptoBubblePixelRadiusProjectionResult`.

The presenter preserves the released success/failure distinction, entry order, instrument association, optional entry index, policy-validation failure reason, and upstream radius-scale projection failure reason. On successful entries it displays only the released optional `radiusCssPixels` evidence and renders `null` as visibly missing.

Gate376 performs no radius arithmetic, no projection, no policy validation, no default selection, and no market-data interpretation.

## Ownership retained below and above Gate376

- Gate375 remains the pure provider-neutral CSS-pixel radius projection owner.
- Gate374 remains the caller-owned pixel-radius bounds contract/validator.
- Gate373 remains the HomeRoute normalized-radius textual-runtime integration owner.
- Gate372 remains the configured-browser normalized-radius textual runtime owner.
- Gate371 remains the normalized-radius textual evidence presenter owner.
- Gate369 and lower released owners retain browser/runtime, normalized-radius, area-weight, freshness and market-truth responsibilities.
- Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

Gate376 does not move route, runtime, hooks/state/effects, renderer, layout, styling, market truth, acquisition, freshness, persistence, or Your Trades authority into the presenter.

## Explicit non-scope retained

Gate376 does **not** own or choose:
- concrete/default minimum or maximum Bubble radii;
- responsive breakpoints or browser viewport measurement;
- collision detection, packing, layout, label-fit, or hit-target policy;
- SVG/canvas/DOM renderer choice;
- Bubble palette, CSS, theme, dimming strength, or motion;
- HomeRoute mutation or a second Live Crypto runtime consumer;
- Binance/provider transport, acquisition, retries, cadence, freshness, universe, ranking, Top-N, stablecoin exclusion, movement, or market-truth semantics;
- persistence, Saved Analysis, Your Trades, journal, chart, Risk/Reward, or Calculation Brain truth.

No concrete visual sizing value is established by this canonical PASS. Those values remain caller/product/design-owned and must not be guessed.

## Next dependency boundary

Fresh Gate376 source now provides the pure presenter for caller-produced pixel-radius projection evidence, while the existing Gate365→Gate369 chain already demonstrates the separate pure app-level view-model then React-hook composition pattern from runtime state into normalized radius evidence.

The smallest dependency-safe technical continuation is therefore a **pure React pixel-radius view-model composition boundary**: accept the released Gate369 `HomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel` plus an exact caller-supplied Gate374 pixel-radius policy; preserve the released runtime state and radius-scale view model by reference; keep pixel-radius projection `null` when no radius-scale projection exists; otherwise delegate once to Gate375 `projectHomeDashboardLiveCryptoBubblePixelRadii` and expose that exact result. It must own no React hook/state/effect, browser clock or viewport API, default radius bounds, renderer/layout/styling, HomeRoute wiring, market/provider semantics, persistence, or Your Trades truth.

A later hook/runtime or visible Bubble renderer still requires caller/product/design sizing inputs. Gate376 does not authorize inventing those values.
