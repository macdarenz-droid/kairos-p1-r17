# Kairos Gate 369 Architecture Addendum

## Canonical authority

`Kairos Controlled Roadmap Gate` #369 / run `34550316082` / job `103111655062` completed full SUCCESS on 2026-09-11 at exact head `7218255caab0724e3bb9abec647005bd04ff8d96`.

Exact canonical candidate:
`KAIROS_HOME_DASHBOARD_LIVE_CRYPTO_BUBBLE_CONFIGURED_BROWSER_RADIUS_SCALE_HOOK_COMPOSITION_FOUNDATION_CANDIDATE_2026-09-11.zip`

Canonical candidate identity: size `1,713,835` bytes; SHA-256 `a0208dd82c2b5e2e0ca9429bc35353deb561c6997bacfa358efa02df170d0136`; Git blob `bc2dbc0d1527ab0e4cff129056f9425630bcf0bf`; root exactly `kairos_p76/`.

Exact-run artifacts:
- `KAIROS_CURRENT_CANDIDATE`: id `10180814114`, wrapper size `1,462,585` bytes, digest `sha256:900d3d506eed7149035fa55bf6ddd34cfb05c2775dc388d77a2bb2b3d30a1f73`.
- `KAIROS_GATE_EVIDENCE`: id `10180814474`, wrapper size `1,425` bytes, digest `sha256:c6c46831b2193f5419abc966c62dbb87af5f6d640a28dbc4cdcbdf54d876bdcf`.

Independent exact-run current-candidate artifact extraction re-proved the candidate filename, exact size/SHA-256, Git blob, clean ZIP integrity, root exactly `kairos_p76/`, no absolute/traversal paths and no symlinks.

Every required canonical stage passed: exact uploaded archive identity/integrity, authoritative Gate368 base/candidate extraction, exact controlled five-file configured-browser radius-scale hook-composition scope, deterministic install, exact Lightweight Charts 5.2.1 proof, production TypeScript compilation, production build, dedicated current/lower-owner verification, focused regression, full unit regression, full controlled-roadmap regression, historical closures, candidate upload and gate-evidence upload.

## Canonically released responsibility

Gate369 releases `src/app/useHomeDashboardLiveCryptoBubbleConfiguredBrowserRadiusScaleViewModel.ts` as the product-configuration-to-browser-radius-scale-hook composition edge.

`useHomeDashboardLiveCryptoBubbleConfiguredBrowserRadiusScaleViewModel(...)` owns only this delegation:
1. accept one exact caller-owned `HomeDashboardLiveCryptoBubbleRuntimeProductConfiguration` reference;
2. delegate that exact configuration once to released Gate360 `composeHomeDashboardLiveCryptoBubbleRuntimeBindingOptions(...)`;
3. pass the exact returned runtime-options object once to released Gate368 `useHomeDashboardLiveCryptoBubbleBrowserRadiusScaleViewModel(...)`; and
4. return the exact released `HomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel` result unchanged.

Gate360 remains runtime product-configuration-to-binding-options composition owner. Gate358 remains browser wall-clock owner. Gate351 remains React lifecycle/state owner. Gate352 remains normalized area-weight arithmetic owner. Gate354 remains area-weight view-model owner. Gate355 remains area-weight React-hook composition owner. Gate364 remains normalized `sqrt(areaWeight)` radius-scale arithmetic owner. Gate365 remains pure radius-scale view-model projection owner. Gate366 remains injected React radius-scale hook-composition owner. Gate368 remains browser clock-injection edge. Gate369 adds only the configured browser composition edge above them.

## Explicit non-scope

Gate369 does **not** own or change:
- product defaults/constants, universe eligibility, ranking, Top-30, stablecoin exclusion, tie-break, neutral movement or freshness thresholds;
- React state/effects/memo/cache or any second lifecycle/state owner;
- browser clocks, `Date.now()`, `new Date()`, cadence, timers or page-visibility behavior;
- provider/request/response/acquisition/retry behavior;
- normalized radius arithmetic beyond consuming released evidence;
- pixel radius, minimum/maximum visible radius, responsive breakpoints, viewport/device geometry, collision/packing/layout, coordinates, labels, interactions or hit targets;
- palette/theme-token selection, CSS, stale/expired opacity, motion or animation;
- `HomeRoute` wiring or route ownership;
- persistence/IndexedDB/Saved Analysis;
- Your Trades Bubble Map, journal/trade truth, chart, Risk/Reward or Calculation Brain.

Live Crypto Bubble Map and Your Trades Bubble Map remain separate authoritative truth domains.

## Next dependency-safe responsibility proof

Fresh exact Gate369 source closes the configured browser radius-scale hook chain, but the released normalized radius evidence still has no React presentation consumer:
- `HomeRoute.tsx` continues to render only `HomeDashboardLiveCryptoBubbleConfiguredTextEvidenceRuntime`;
- `HomeDashboardLiveCryptoBubbleTextEvidenceRuntime.tsx` still consumes the earlier Gate355 area-weight hook, not the Gate369 radius-scale hook;
- `HomeDashboardLiveCryptoBubbleTextEvidence.tsx` exposes released runtime/market/area-weight evidence only and does not consume `radiusScaleProjection`; and
- a fresh source search finds radius-scale references only in the released radius-scale model/hooks plus their tests/verifiers, with no route or presentation component consuming the radius-scale view model.

Moving directly from this plumbing edge to visible pixel circles would collapse two still-separate responsibilities: consuming normalized radius evidence and choosing concrete pixel/layout policy. The user-approved product contract defines `24h quote volume` as Bubble-size truth and permits nonlinear radius scaling, but it does not establish concrete minimum/maximum pixel radii, responsive breakpoints, collision packing or label-fit values. Those values must not be invented.

Therefore the smallest dependency-safe next responsibility is a **pure React normalized-radius evidence presentation boundary** only:
1. accept one exact released `HomeDashboardLiveCryptoBubbleReactRadiusScaleViewModel` reference as a prop;
2. preserve and expose the released runtime status/failure evidence without creating runtime state;
3. preserve projection success/failure/null semantics and exact entry order;
4. expose each released instrument association and exact `radiusScale` evidence, including `null` as missing, without recomputing `sqrt(areaWeight)` or market metrics; and
5. choose no hook, state/effect/memo/cache, browser clock, product/provider policy, pixel dimensions, viewport/collision/layout/labels, palette/CSS/motion, route wiring, persistence or Your Trades behavior.

This presenter is a safe reversible bridge from canonically released normalized radius evidence to React presentation. A later controlled runtime-composition slice can consume Gate369 exactly once and feed this presenter without creating a second acquisition/state owner. Concrete pixel-circle policy remains later and must be separately evidence-backed.