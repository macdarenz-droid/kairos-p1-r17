# npm 10.9 stops with "Cannot read properties of null (reading 'edgesOut')" on a fresh vitest 4.1.11 install
# (a peer loop through vite 8's optional devtools). The peers are listed in package.json instead.
legacy-peer-deps=true
